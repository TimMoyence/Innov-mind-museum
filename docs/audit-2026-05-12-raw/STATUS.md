# Audit 2026-05-12 RAW — STATUS tracker

> **Companion file** to `FINAL-REPORT.md`. The audit report stays **immutable as historical record** — closure tracking lives here.
>
> **Last update :** 2026-05-14 — Tim (/team run `2026-05-14-app-store-readiness-batch` closed P0 #6 + #7 + #8 ; audit `2026-05-14-verification-batch` re-verified Garak + Anonymize + Stryker claims ; previously /team runs `2026-05-14-mobile-infra-hardening-batch` closed P0 #4 + #5 + #10 + #11 and `2026-05-14-security-batch-p0-typeorm-sharp` closed P0 #2 + #3)
> **Scope :** 14 P0 launch blockers identifiés dans FINAL-REPORT §3.1 + Risk Heat Map §9.

## P0 closure status

| # | Finding | Statut | Date | Source / commit |
|---|---|---|---|---|
| 1 | iOS 26 / A18 Pro crash | ✅ **RESOLVED** | 2026-05-14 | Expo SDK upgrade tenté → downgrade vers version known-good. App fonctionnelle iOS 26.x. Instrumentation conservée jusqu'au 2026-06-15 bake (`AppDelegate.swift:93`, `IOS26_CRASH_DIAG.md`, ADR-004). Memory `project_ios26_crash_investigation` mise à jour. |
| 2 | TypeORM `.set({ undefined })` replayable tokens | ✅ **RESOLVED** | 2026-05-14 | /team run `2026-05-14-security-batch-p0-typeorm-sharp`. 3 sites patched (`consumeResetTokenAndUpdatePassword`, `consumeEmailChangeToken`, `updatePassword`) → `() => 'NULL'`. Unit tests updated (49/49 pass), 2 e2e replay regression tests added (`auth-reset-password.e2e.test.ts`, `auth-change-email.e2e.test.ts`), new ESLint rule `musaium-test-discipline/no-typeorm-set-undefined` blocks future regressions. Reviewer verdict APPROVED (weightedMean 92.6/100). |
| 3 | Sharp `limitInputPixels` DoS | ✅ **RESOLVED** | 2026-05-14 | /team run `2026-05-14-security-batch-p0-typeorm-sharp`. 6 sharp() sites in `image-processing.service.ts` (5) + `image-preprocess.ts` (1) now carry `{ limitInputPixels: 24_000_000, failOn: 'error' }` via shared `SHARP_DECODE_OPTIONS` constant. Unit tests added (oversize PNG → `ImageDecodeError`, normal JPEG → pass). |
| 4 | Cert pinning `PLACEHOLDER_SPKI_HASHES_TBD_PROD` | ✅ **RESOLVED** | 2026-05-14 | /team run `2026-05-14-mobile-infra-hardening-batch`. Hostname corrigé `api.musaium.app` → `musaium.com` (le placeholder pointait vers un domaine inexistant). SPKI réels capturés via openssl contre `musaium.com:443` : leaf `ZDRgYM8cmWD/…=` (LE, exp 2026-06-19) + intermediate `iFvwVyJSxnQdyaUvUERIf+…=` (LE E8, exp 2027-03-12, absorbe rotations LE 90 j). `EXPO_PUBLIC_CERT_PINNING_ENABLED=true` flippé dans `.env`, `.env.production.example`, `.env.preview.example`. Runbook 7-step + script `scripts/capture-spki.sh` ajoutés (`museum-frontend/docs/CERT_PINNING_RUNBOOK.md`). Tests intégration adaptés. Smoke device manuel à exécuter avant store-submission. |
| 5 | `chatSessionStore` plaintext AsyncStorage | ✅ **RESOLVED** | 2026-05-14 | /team run `2026-05-14-mobile-infra-hardening-batch`. Option (a) appliquée per audit R12 §8.4 : `persist` middleware retiré, store in-memory only. Cold-start chat re-hydrate via `useSessionLoader.ts:30` → `chatApi.getSession()` (acceptable online-first V1, +200-400ms). PII messages plus jamais sur disque plaintext. Eviction cap (10 sessions) préservée comme borne mémoire. Test mock AsyncStorage retiré ; tous tests passent (chatSessionStore 14/14, useSessionLoader, useChatSession). V1.1 path : encrypted-persister via expo-secure-store / encrypted MMKV si offline chat devient feature ask. |
| 6 | AiConsentModal AI provider non nommé (AI Act 5.1.2(i)) | ✅ **RESOLVED** | 2026-05-14 | /team run `2026-05-14-app-store-readiness-batch`. Nouvelle clé `consent.data_providers` ajoutée dans les 8 locales (en/fr/es/de/it/ja/zh/ar), naming aligné verbatim sur `museum-web/src/lib/privacy-content.ts:60,117,178,235` (OpenAI, Google, DeepSeek). Rendu dans `AiConsentSheetContent.tsx` comme 6e bullet du bloc `data_shared` avec icône `cloud-outline`, parité visuelle avec les 5 bullets existants. Apple Guideline 5.1.2(i) (Nov 2025) + EU AI Act Art. 50 satisfaits. `node scripts/check-i18n-completeness.js` PASS (753 clés × 8 locales). |
| 7 | UIBackgroundModes mismatch | ✅ **RESOLVED** | 2026-05-14 | /team run `2026-05-14-app-store-readiness-batch`. Décision actée dans design.md ADR-batch-2 : voice = core feature, on **garde** `UIBackgroundModes=audio` mais on **wire la capability**. `useTextToSpeech.ts:222-229` appelle `setAudioModeAsync({ shouldPlayInBackground: true, playsInSilentMode: true })` au mount (native-only, web skip). `useAudioRecorder.ts:118,162` étendu avec `shouldPlayInBackground: true` sur les 2 transitions (start + stop recording) pour que le playback post-record reste backgroundable. `app.config.ts` mirror `UIBackgroundModes: ['audio']` dans `ios.infoPlist` → prochain `expo prebuild --clean` ne régressera pas. Android `FOREGROUND_SERVICE_MEDIA_PLAYBACK` + `AudioControlsService` déjà OK. Plus de mismatch capability ↔ implémentation (Apple Forum #95216 reject évité). |
| 8 | StoreButton `href='#'` | ✅ **RESOLVED** | 2026-05-14 | /team run `2026-05-14-app-store-readiness-batch`. Hybrid : **Apple** wire la vraie URL `https://apps.apple.com/fr/app/musaium/id6761004268` (fournie par PM, app ID 6761004268). **Google Play** rendu en état "Coming soon" disabled (Play Store URL pas encore disponible). `StoreButton.tsx` étendu avec prop `disabled?: boolean` → quand `true`, render `<span role="link" aria-disabled="true">` au lieu de `<a href>`, `cursor-not-allowed`, `opacity-55`. Dictionnaires FR/EN enrichis : `download.appStoreHref` + `download.googlePlayComingSoon` ("Coming soon" / "Bientôt disponible"). `LandingDownloadCTA.tsx` passe `href` à Apple, `disabled` + nouveau subLabel à Google. Type `Dictionary.download` (lib/i18n.ts) étendu. Tests Header/Footer/snapshots/a11y mis à jour avec les 2 nouvelles clés ; `StoreButton.test.tsx` nouveau test dédié pour l'état disabled (role link, aria-disabled, span tag, pas de href). Cohérent avec doctrine `feedback_no_feature_flags_prelaunch` (live ou revert, pas de half-baked notify-me). C6.3 reste sur sa propre track roadmap. |
| 9 | `admin/users/[id]` stub `---` | 🔴 OPEN | — | Session 5 Web-Admin |
| 10 | `expo-image` absent (10 sites `<Image>` RN) | ✅ **RESOLVED** | 2026-05-14 | /team run `2026-05-14-mobile-infra-hardening-batch`. `expo-image@55.0.10` installé via `npx expo install` (SDK 55 compat) + plugin déclaré dans `app.config.ts`. Codemod sur **7 sites réels** (audit overestimait à 10) : `ImageCompareCard`, `ImageCarousel`, `ChatInput`, `VisitSummaryModal`, `ImageSection`, `MuseumSheetEnrichmentBody` + `imageUploadOptimization.ts`. `resizeMode="cover"` → `contentFit="cover"` ; `recyclingKey` ajouté sur tous les sites en liste/recyclage FlashList ; `cachePolicy="memory-disk"` (memory pour preview locale) ; transition fade-in 150ms sur ImageCompareCard. `Image.getSize()` callback (RN) → `Image.loadAsync({ uri })` Promise (expo-image, retourne `ImageRef.{width,height}`) — l'API `Image.getMeta()` mentionnée dans le brief n'existe pas en SDK 55, `loadAsync` est l'équivalent canonique. Snapshot ImageCompareCard regénéré. Tests 2120/2120 ✅. |
| 11 | `android/sentry.properties:3 project=apple-ios` | ✅ **RESOLVED** | 2026-05-14 | /team run `2026-05-14-mobile-infra-hardening-batch`. `museum-frontend/android/sentry.properties:3` → `defaults.project=android`. Aligne avec `eas.json:44 SENTRY_PROJECT="android"` (canonical pour builds EAS Android). Fix bloque la fuite silencieuse de sourcemaps Android vers le projet Sentry iOS lors d'invocations `sentry-cli` hors-EAS (dev-menu source-map upload). |
| 12 | RTL Arabic broken (8+ left/right vs start/end) | 🔴 OPEN | — | Session 4 i18n-a11y-EAA |
| 13 | MFA page hard-coded English | 🔴 OPEN | — | Session 4 i18n-a11y-EAA |
| 14 | EAA non-conformant (€25k/an FR) | 🔴 OPEN | — | Session 4 i18n-a11y-EAA |

**Score :** 10/14 closed (iOS 26 + TypeORM replayable tokens + Sharp DoS + cert pinning + chatSession plaintext + AI provider disclosure + UIBackgroundModes capability + StoreButton hrefs + expo-image migration + sentry Android project).
**Remaining :** 4 P0s à fermer avant 2026-05-31 GO/NO-GO checkpoint (#9 admin/users stub, #12 RTL Arabic, #13 MFA i18n, #14 EAA).

## Out-of-scope adjacents promus blocker pré-launch

| Item | Catégorie | Statut | Cible |
|---|---|---|---|
| EU CRA VDP (deadline 2026-09-11) | Compliance / disclosure | ✅ **RESOLVED** 2026-05-14 | F6 templates landed : `SECURITY.md` racine + `museum-web/public/.well-known/security.txt` (RFC 9116, expires 2027-05-14) + page Next.js `/security` FR/EN (route + dictionary `footer.links.security` + footer link + sitemap entry + `lib/security-content.ts`) + `docs/operations/VDP_RUNBOOK.md` (triage solo founder + GDPR 72h + CRA 24h/72h/14d timelines + escalation matrix + 6 templates A-F) + `docs/legal/SUBPROCESSORS.md` pointeur vers le ledger canonique `docs/compliance/SUBPROCESSORS.md` (P0-3 DeepSeek consistency déjà ack). Domaine `musaium.com` (cf. cert-pinning runbook). ENISA SRP onboarding + CERT-FR test-run à tracker dans ROADMAP_TEAM avec deadline 2026-09-11. |
| Garak target Phi-3 PAS Musaium | AI safety verification | ✅ **RESOLVED** 2026-05-14 | /team run `2026-05-14-garak-musaium-rest-swap`. `.github/workflows/llm-security-garak.yml` réécrit sur le pattern de `llm-security-promptfoo.yml` : services pgvector + redis, migrations, `pnpm dev`, wait-for-health, bootstrap throwaway visitor (`garak+ci@musaium.test`), session-per-probe freshness loop (6 probes : `promptinject,leakreplay,encoding,dan,tap,xss`), `--target_type rest --generator_option_file museum-backend/scripts/llm-security/musaium-garak-rest.json`, `--parallel_attempts 1`. Nouveau step **Content check** scanne chaque `*.report.jsonl` et fail si zero record carries `output` non-vide (anti-silent-drift R-1). Severity-eval Python widening 1→6 fichiers via `glob.glob('musaium-garak-*.report.jsonl')`. Phi-3 baseline + `huggingface.Pipeline` target purgés. Corrections contract vs brief utilisateur (UFR-013) : `$.message.text` (pas `$.assistantMessage.content`), body `{"text":"$INPUT"}` (pas `{content,locale}`), `.session.id` (pas `.id` — promptfoo workflow porte une bug latent à corriger en follow-up). Probes `dan,encoding,tap,xss` ajoutent couverture G2 (multi-turn) + G4 (encoding) + LLM05. ADR-049 §Rollout Phase 1.5 closed ahead of schedule. Coverage gap connu Phase 2 : LLM Guard sidecar pas in CI (no service container) — promptfoo identique. PR # + first workflow run URL : TBD Tech Lead post-merge. |
| Stryker `99.75%` claim non vérifié | Test discipline | ✅ **CORRECTED** 2026-05-14 | Audit `2026-05-14-verification-batch`. `99.75 %` non reproductible depuis aucun artifact committé (F4 forensic confirme). `PHASE_HISTORY.md:32-36` réécrit avec nombres vérifiés : snapshot 2026-05-11 = 0 survivors / 4067 covered / 100 % covered / 89.42 % classical ; snapshot courant `reports/stryker-incremental.json` mtime 2026-05-14 11:42 (post-commit `07aea6ef`) = Killed 1387 / Timeout 3190 / Survived 28 / NoCov 298 / Ignored 701 / RuntimeError 9, soit **93.35 % classical / 99.39 % covered**. Les 28 survivors = backlog mutation prochain sprint. Aucun changement de code source nécessaire. |
| Anonymize PII scrubbing chaîne brisée | AI safety / GDPR | ✅ **RESOLVED** 2026-05-14 | /team run `2026-05-14-pii-redaction-llm02`. Callback type de `evaluateGuardrailProvider` élargi (`run: () => Promise<{allow, reason?, redactedText?}>`), `InputGuardrailResult` étend `redactedText?`, `PrepareReady.redactedText?` ajouté, `ChatMessageService.postMessage`+`postMessageStream` substituent `effectiveUserText = prep.redactedText ?? input.text` AVANT `piiSanitizer.sanitize()` → `OrchestratorInput.text` (donc LLM cache + history sanitize en aval). Nouvel audit hash-chained `AUDIT_GUARDRAIL_INPUT_REDACTED` (`metadata.pii_redacted: true`, `placeholder_count`, sha256 du redactedText uniquement) émis via `buildGuardrailInputRedactedAuditEntry`. Compteur Prometheus `musaium_guardrail_pii_redacted_total{locale,placeholder_type}` (cardinalité ≤108). Tests : 5 unit R1/R2/R5/R7/R8 (`guardrail-evaluation-service.test.ts`), test R6 audit-payload no-leak (`guardrail-audit-payload.test.ts`), E2E `chat-pii-redaction.e2e.test.ts` (harness étendu w/ `guardrailProviderOverride` + `auditServiceOverride`). Out-of-scope V1 : history rétroactive (les ChatMessage persistés AVANT ce commit restent en clair en BDD ; RGPD right-to-erasure intact via cascade delete). Pas de feature flag (live ou revert). |

## Lecture rapide

- **Avant chaque session /team** : relire la ligne correspondante + le détail file:line dans FINAL-REPORT §3.1 + le rapport forensique référencé (F-* dans `05-gaps/` ou R-* dans `04-research/`).
- **Au merge d'un fix** : éditer ce fichier (statut + date + commit/PR), pas FINAL-REPORT.
- **GO/NO-GO 2026-05-31** : tableau ci-dessus doit afficher 14/14 ✅.
