{
  "path": "./CLAUDE.md",
  "verdict": "UPDATE",
  "confidence": 9,
  "valueLevel": "MEDIUM",
  "reason": "Required file, mostly accurate doctrine + gotchas, but bloated 425 lines × every-session = high token toll. Several line numbers/counts drifted. Pièges section grown unbounded.",
  "staleClaims": [
    {"line": 124, "claim": "museum-frontend/shared/api/generated/openapi.ts | 83 KB / 3 510 lines", "actual": "Actual: 4831 lines (file grew ~37%). Still auto-generated but size figure stale."},
    {"line": 126, "claim": "museum-backend/src/data/db/migrations/*.ts (56 files)", "actual": "Actual: 64 .ts migration files. Count drifted by 8."},
    {"line": 149, "claim": "apiRouter monté à `/api` dans `museum-backend/src/app.ts:233`", "actual": "Actual line is app.ts:262 (`app.use('/api', createApiRouter(...))`). Off by ~29 lines."},
    {"line": 164, "claim": "CORS allowedHeaders contient déjà `sentry-trace` + `baggage` (museum-backend/src/app.ts:116-117)", "actual": "Actual lines are 147-148. Off by ~31. Middleware mount also claimed at app.ts:103, actual line 132."},
    {"line": 88, "claim": "ci-cd-backend.yml ... → deploy prod (push main) / staging (push staging)", "actual": "unverified — but contradicts MEMORY 'project_no_staging_v1' which says no staging server until B2B revenue. Likely stale or staging means env-target not server."}
  ],
  "consolidateTarget": null,
  "updatePlan": "1) Drop hardcoded line numbers from Pièges (lines 149, 164) — use file paths only; line numbers rot fast. 2) Refresh openapi.ts size (now ~4831 lines) + migrations count (64). 3) Move 5 oldest/most-niche Pièges (PgBouncer LISTEN/NOTIFY, SWC Relation<T>, Prometheus var-expand, SigLIP normalize, nginx proxy_pass var) to a new docs/GOTCHAS_ARCHIVE.md and link — reduce always-loaded token cost. 4) Voice V1 section (322-333) is detailed enough to live in docs/AI_VOICE.md only — keep 2-line pointer here. 5) Verify staging claim in CI section vs no-staging memory.",
  "deleteRationale": null,
  "wouldLoseInfo": "Pièges connus carry hard-won incident knowledge — if pruned, MUST move to a referenced GOTCHAS doc, not deleted. Specifically iOS Pods/, RTL props, Stryker handles, TypeORM set-undefined, workspace file: package — all cite real incidents."
}
