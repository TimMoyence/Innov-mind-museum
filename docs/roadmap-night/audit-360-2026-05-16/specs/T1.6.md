# T1.6 — Barrel doctrine FE : enforce OU strip docblock (UFR-013)

**Status**: SPEC_READY (NO-OP recommandé — voir vérifications)
**Source task**: `docs/roadmap-night/audit-360-2026-05-16/S1-types-libs.tasks.md` § T1.6 (lignes 127-146)
**Source audit**: `.claude/skills/team/team-reports/working/2026-05-16-audit-360/S1-types-libs-dryskiss.md` (introuvable au moment de la spec — pas de section barrel-FE retrouvée par grep `barrel|MUST go through|13 barrel`)
**Date**: 2026-05-16

---

## Spec

### Intent (UFR-013 — docblocks ≠ mensonge)
Vérifier que les docblocks des barrels `features/*/index.ts` ne mentent pas sur le contrat d'import (claim « MUST go through barrel » alors que la réalité montre un ratio massif de deep imports). Décider : (a) enforce le contrat par ESLint + backfill, ou (b) réécrire les docblocks honnêtement.

### Acceptance Criteria (verbatim YAML T1.6 lignes 135-139)
> AU CHOIX :
> (a) Enforce : ajouter ESLint `no-restricted-imports` per feature scope, backfill les 13 barrels (auto-codemod si possible), 0 deep import résiduel
> (b) Strip docblocks : retirer/réécrire les claims « MUST go through barrel » dans les 13 `features/*/index.ts` pour refléter réalité (« deep imports allowed within reasonable scope »)
> Décision documentée. UFR-013 (docblocks ne doivent pas mentir).

### Scope boundary
- **AUDIT-ONLY (path c — NO-OP)** : aucune édition code. Le travail a déjà été fait commit `968cdafad` 2026-05-13 (3 jours avant la rédaction du YAML T1.6 le 2026-05-16). Spec documente la stale-task et propose fermeture sans action.

---

## Vérifications préalables (UFR-018)

### Barrels existants (claim YAML : « 13 barrels » — FAUX)

Commande : `find museum-frontend/features -maxdepth 2 -name "index.ts" -type f`

Top-level (`features/*/index.ts`) — **3 barrels seulement**, pas 13 :
1. `museum-frontend/features/auth/index.ts` (20 lignes)
2. `museum-frontend/features/chat/index.ts` (29 lignes)
3. `museum-frontend/features/museum/index.ts` (17 lignes)

Nested deeper (`features/<name>/<layer>/.../index.ts`) — 4 sub-barrels, hors scope du contrat « public feature API » :
4. `features/chat/application/sendStrategies/index.ts` (re-exports purs, pas de docblock contrat)
5. `features/chat/infrastructure/chatApi/index.ts` (docblock honnête, autorise explicitement les direct module imports)
6. `features/chat/ui/bottom-sheet-router/index.ts` (re-exports purs)
7. `features/chat/ui/bubbleSections/index.ts` (re-exports purs)

Liste features sans `index.ts` (10/13) : `art-keywords`, `conversation`, `daily-art`, `diagnostics`, `home`, `legal`, `onboarding`, `paywall`, `review`, `settings`, `support`.

Conclusion : le YAML compte 13 features mais traite chaque feature comme ayant un barrel, ce qui est faux — seulement 3 features ont effectivement un `index.ts` top-level.

### Docblocks « MUST go through barrel » (claim YAML : tous mentent — FAUX)

Commande : `grep -rln "MUST.*barrel\|MUST go through" museum-frontend/features/`
Résultat : **0 fichier** — aucune occurrence.

Verbatim docblock `auth/index.ts:1-14` (extrait) :
> Auth feature surface — re-exports for callers that want a stable entry point.
> Deep imports into `application/`, `infrastructure/`, `domain/` are allowed (and currently the dominant pattern across the codebase — see `features/README.md`). [...]
> History: the previous docblock claimed cross-feature imports "MUST" go through this barrel. Audit 2026-05-12 (P1-6) measured 265 cross-feature deep imports vs 8 barrel imports (~33:1). The doctrine was untruthful per UFR-013 and was retired — barrels are now opt-in, not mandatory.

Verbatim `chat/index.ts:13-17` (extrait) : « History: the previous docblock claimed conversation/museum "MUST" import from this barrel. [...] The doctrine was untruthful per UFR-013 and was retired — barrels are now opt-in, not mandatory. »

Verbatim `museum/index.ts:9-12` (extrait) : « History: the previous docblock claimed cross-feature imports "MUST" go through this barrel. [...] retired — barrels are now opt-in, not mandatory. »

Conclusion : les 3 docblocks reflètent **déjà** la réalité honnête + citent UFR-013 + référencent l'audit P1-6 + invitent à `features/README.md`. **Aucune édition requise.**

### Ratio deep vs barrel imports (claim YAML : « 203 vs 8 = 25:1 » — sous-estimé)

Commandes (depuis `museum-frontend/`) :
- `grep -rE "from ['\"]@/features/<name>['\"]" . --include='*.ts' --include='*.tsx'` (barrel)
- `grep -rE "from ['\"]@/features/<name>/" . --include='*.ts' --include='*.tsx'` (deep)

Résultats spot-check (verbatim wc -l) :

| Feature | Barrel imports | Deep imports | Ratio |
|---|---:|---:|---:|
| auth | 0 | 37 | ∞:0 (aucun consommateur via barrel) |
| chat | 5 | 243 | 49:1 |
| museum | 2 | 74 | 37:1 |
| **Total** | **7** | **354** | **51:1** |

Claim YAML « 203 vs 8 = 25:1 » : sous-estime massivement (réalité ~354 vs 7 = ~51:1). Commit `968cdafad` mentionnait déjà cette divergence (« The audit's headline ratio of 25:1 (203 vs 8) under-counted; the truthful figure is worse »).

### ESLint existing rules

Commande : `grep -n "no-restricted-imports\|no-internal-modules" museum-frontend/eslint.config.mjs`
Résultat : **0 match**. Ni `no-restricted-imports` ni `import/no-internal-modules` n'est configuré. La discipline d'import barrel n'est pas enforcée par tooling.

### Git archéologie

Commande : `git log --oneline -20 -- museum-frontend/features/{auth,chat,museum}/index.ts`
Verbatim :
```
968cdafad refactor(frontend,P1-6): retire barrel doctrine — align docs to actual 33:1 imports
bdc3e60e9 refactor(frontend): cross-feature smell — auth + museum public APIs
933400a44 feat(multi): P0 museum intro + P1 content preferences + SSE fix + tech-debt cleanup
```

Commit `968cdafad` 2026-05-13 (3 jours avant le YAML T1.6 du 2026-05-16) a déjà appliqué le path (b) du YAML. Message commit verbatim § rationale : « Per UFR-013 (truth-telling) and feedback_no_feature_flags_prelaunch ("live or revert"), retire the doctrine: rewrite the three docblocks to state honestly that deep imports are allowed and barrels are opt-in entry points for symbols that have become stable cross-feature API. Exports themselves unchanged; no caller is rewritten. »

---

## Design

### Path (a) — Enforce ESLint
- Étapes : ajouter `no-restricted-imports` (ou `import/no-internal-modules`) per feature scope + backfill 10 barrels manquants + élargir les 3 existants + codemod ~354 import statements.
- Risque : volume énorme, contradiction directe avec la décision explicite de `chat/index.ts:24-27` (« Full chatApi is intentionally NOT re-exported — outside consumers should only use the session-list endpoints the dashboard needs. Streaming, messaging, media, and TTS stay private to the chat feature. »). Path (a) **réintroduirait** ce qui a été délibérément refusé.
- Effort réaliste : 2-4 jours (≥354 sites + 10 nouveaux barrels + lint config + tests).
- Justification J-16 launch : **NON tenable** (et antagoniste au design product actuel).

### Path (b) — Strip docblocks
- Étapes : **DÉJÀ FAIT** par commit `968cdafad` 2026-05-13.
- Aucune édition supplémentaire utile : les docblocks citent déjà UFR-013, citent le ratio mesuré (~33:1), invitent à `features/README.md`, et qualifient les barrels d'opt-in.

### Path (c) — NO-OP + fermeture audit-360 task
- Étapes : aucune. Le YAML T1.6 a été rédigé sur source stale (audit 2026-05-12 P1-6 carry-over) sans re-vérifier que P1-6 avait été résolu commit `968cdafad`.
- Effort : 0.
- Action proposée : marquer T1.6 résolu dans `tracking.md` (ou équivalent) avec pointeur vers commit `968cdafad` + cette spec.

### Recommandation finale

**Path (c) NO-OP**. Justifications :
1. UFR-018 (check before assuming) : la vérification grep démontre que la prémisse du YAML T1.6 (« 13 barrels avec docblock MUST ») est factuellement fausse. Les 3 barrels existants sont déjà honnêtes.
2. UFR-013 (truth-telling) : déjà satisfait par commit `968cdafad`.
3. UFR-001 (product-first) + J-16 : path (a) coûteux + antagoniste au design `chat/index.ts:24-27` ; aucun ROI launch.
4. UFR-016 (bury dead code) : pas applicable — il n'y a pas de code mort à enterrer ; il y a une **tâche morte** à fermer.

Path (a) reste documentable comme entrée TECH_DEBT post-V1 si la philosophie change (cf. Q3).

---

## Tasks (path c retenu)

- **T1.6.a** — Vérifier (déjà fait dans cette spec) : `grep -rln "MUST.*barrel" museum-frontend/features/` → 0 fichier. Confirme path (b) déjà appliqué.
- **T1.6.b** — Marquer T1.6 = `RESOLVED-PRE-EXISTING` dans `docs/roadmap-night/audit-360-2026-05-16/tracking.md` avec pointeur commit `968cdafad` + cette spec.
- **T1.6.c** — (optionnel, Q3) Si la décision est de retenter le path (a) post-V1, créer entrée `TECH_DEBT.md` avec scope verbatim ci-dessus + estimate 2-4j + dépendance "post-V1-2026-06-01".
- **T1.6.d** — (anti-régression léger) Ajouter à `features/README.md` une note pointant explicitement vers les 3 barrels existants + leur philosophie « opt-in narrow surface » pour éviter une future tâche stale identique. Effort < 10 min.

Tasks YAML originales (a/b) **non exécutées** : path (a) NON tenable J-16 + antagoniste design ; path (b) DÉJÀ FAIT commit `968cdafad`.

---

## Open Questions

- **Q1 — Path (a) vs (b) vs (c) ?**
  Recommandation : **(c) NO-OP**. Justifications : prémisse YAML factuellement fausse (3 barrels ≠ 13, 0 docblock ment), path (b) déjà appliqué commit `968cdafad` (verbatim git log), path (a) antagoniste au design explicite `chat/index.ts:24-27`. Toute action additionnelle violerait UFR-001 (overengineering pre-launch) et UFR-013 (claim work qu'on ne fait pas).

- **Q2 — Texte de remplacement docblock ?**
  Sans objet — les docblocks actuels sont déjà honnêtes et meilleurs que la proposition YAML (« deep imports allowed within reasonable scope »). Ils citent le ratio mesuré, citent UFR-013, citent l'audit P1-6, et qualifient les barrels d'opt-in. Aucune réécriture n'améliorerait.

- **Q3 — Documenter post-V1 path-a comme TECH_DEBT ?**
  Recommandation : **non par défaut**, sauf si l'équipe veut explicitement revenir à une doctrine barrel-mandatory. Raison : commit `968cdafad` n'est pas un « postpone », c'est un **rejet motivé** de la doctrine (cf. rationale commit verbatim). Re-ouvrir un TECH_DEBT en contradiction de cette décision serait incohérent. Si la philosophie change post-V1 (B2B exige isolation forte des features), créer alors une ADR plutôt qu'un TD.

- **Q4 (nouvelle)** — Le YAML T1.6 a été rédigé sur source stale ; combien d'autres tâches S1 ont le même bug ? À reviewer dans `tracking.md` au minimum les tâches qui réfèrent l'audit 2026-05-12 carry-over (T1.6 ref ligne 134 mentionne explicitement « docs/audit-2026-05-12/MASTER.md (P1-6 carry-over) »).
