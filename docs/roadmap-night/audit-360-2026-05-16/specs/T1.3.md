# T1.3 — Documenter noUncheckedIndexedAccess BE en TECH_DEBT

**Status**: SPEC_READY
**Source task**: `docs/roadmap-night/audit-360-2026-05-16/S1-types-libs.tasks.md` § T1.3
**Source audit**: `.claude/skills/team/team-reports/working/2026-05-16-audit-360/S1-types-libs-dryskiss.md` § 4.2
**Date**: 2026-05-16

---

## Spec

### Intent
Documenter par écrit la décision de défer l'activation de `noUncheckedIndexedAccess` côté backend (option TS absente confirmée dans `museum-backend/tsconfig.json`) afin d'empêcher tout drift silencieux post-launch. Action de documentation uniquement — pas d'activation, pas de fix de site.

### Acceptance Criteria
Verbatim recopié de la task YAML (T1.3 lignes 68-69) :
> `docs/TECH_DEBT.md` reçoit une nouvelle entrée TD-BE-noUncheckedIndexedAccess avec : (1) raison du défer pré-launch (35-50 sites, 8-12h), (2) sites estimés (similarity.service.ts, chat.repository.ts pagination, validators arrays), (3) deadline post-V1 sprint 1, (4) trigger (toute mention noUncheckedIndexedAccess en review). Entrée datée 2026-05-16.

Précisions mesurables (pour le green-code-agent) :
- L'entrée DOIT respecter le format existant `### TD-<num> — <titre>` documenté en § Convention (lignes 11-21 de TECH_DEBT.md).
- L'ID = **`TD-16`** (next free après TD-15, le suffixe `BE-noUncheckedIndexedAccess` mentionné dans le YAML est descriptif, pas un nouveau schéma — le tracker existant utilise numérique séquentiel).
- L'entrée DOIT contenir les bullets suivants (mapping AC → format existant) :
  - `- [ ] **Statut** : ouvert (créé 2026-05-16, audit S1 § 4.2)`
  - `- **Référence code** :` (avec `museum-backend/tsconfig.json:1-35` — option absente)
  - `- **Symptôme** :` (Why = type-safety drift vs FE/Web)
  - `- **Pourquoi non résolu pré-launch** :` (35-50 sites × 8-12h, P0 langfuse + JWKS prioritaires per audit § 4.2)
  - `- **Sprint d'origine** : audit 2026-05-16 (S1 § 4.2)`
  - `- **Effort estimé** : 8-12h` (per audit § 4.2)
  - `- **Trigger** :` (toute mention `noUncheckedIndexedAccess` en review code OU lecture audit 05-16)
  - `- **Deadline** :` (post-V1 sprint 1, soit fenêtre 2026-06-01 → 2026-06-30)
  - `- **Owner** :` (à laisser blank ou `Tim` — single-dev pre-launch)
  - `- **Comment fermer** :` (étapes numérotées)
- Date `2026-05-16` PRESENT dans l'entrée.
- L'entrée s'insère AVANT la section `## Tech debts fermés` (ligne 397 actuelle de TECH_DEBT.md), donc après TD-15.
- Pas d'autre modification de fichier (zéro touch sur `tsconfig.json`, zéro touch sur code BE).

### Scope boundary
- **TOUCHE** : `docs/TECH_DEBT.md` uniquement (ajout d'un bloc ~30 lignes).
- **NE TOUCHE PAS** : `museum-backend/tsconfig.json`, aucun fichier `.ts` BE, aucun test, aucun CI workflow, aucun autre `docs/*.md`.
- **NE COMMIT PAS** d'activation : ceci est un acte de documentation pur. L'activation est explicitement **P1 post-V1**.

---

## Design

### Vérifications faites au moment du spec (UFR-013)
- `museum-backend/tsconfig.json` lu intégralement : `noUncheckedIndexedAccess` **CONFIRMÉ ABSENT** (49 lignes, aucune occurrence). Verdict audit § 4.2 = correct.
- Format des entrées existantes vérifié sur TD-7 (lignes 166-195) et TD-12 (lignes 285-302) comme référence.
- Numéro d'entrée libre = TD-16 (TD-1..TD-15 utilisés, dernière entrée ouverte TD-15).
- Section d'insertion = juste après TD-15 ligne 393, avant `## Tech debts fermés` ligne 397.

### Sites estimés (transparence sur la qualité de l'estimation)
Les **3 fichiers explicitement nommés** par l'audit § 4.2 et la task YAML AC :

| Fichier | Existence vérifiée | `[0]` raw count | Note |
|---|---|---|---|
| `museum-backend/src/modules/chat/useCase/visual-similarity/similarity.service.ts` | ✅ existe | 0 occurrences directes `[0]` | Le finding Subagent A repose probablement sur des patterns d'indexing indirects (`topK.slice`, `.at()`, destructuring) — à confirmer à l'activation |
| `museum-backend/src/modules/chat/adapters/secondary/persistence/chat-repository-queries.ts` | ✅ existe | 0 occurrences directes `[0]` | Pagination probable via TypeORM query builder — sites à découvrir au moment de l'activation |
| `museum-backend/src/modules/chat/adapters/secondary/persistence/chat.repository.typeorm.ts` | ✅ existe | 0 occurrences directes `[0]` | Idem |
| Validators arrays (cf `museum-backend/src/shared/db/jsonb-validator.ts`, `museum-backend/src/modules/chat/useCase/orchestration/sources-validator.ts`) | ✅ 2 fichiers existent | non profilé | Mentionné catégoriquement dans la task YAML AC |

Signal BE-wide : **38 fichiers BE contiennent au moins un `[0]`** (`grep -rln "\[0\]" museum-backend/src/`). 68 occurrences brutes au total. L'estimation 35-50 sites de l'audit est **plausible** mais nécessite un comptage rigoureux à l'activation (ce qui n'est PAS le scope T1.3).

→ Le spec maintient l'estimation 35-50 sites / 8-12h **telle quelle** (per audit § 4.2 + task YAML AC), avec une note de transparence dans la closure note.

### Format proposé de l'entrée (template à insérer)
```markdown
### TD-16 — `noUncheckedIndexedAccess` absent côté backend (drift vs FE/Web)

- [ ] **Statut** : ouvert (créé 2026-05-16, audit S1 § 4.2)
- **Référence code** :
  ```
  museum-backend/tsconfig.json:1-35   # compilerOptions.noUncheckedIndexedAccess ABSENT
  museum-frontend/tsconfig.json       # noUncheckedIndexedAccess: true (cf audit § 4.1)
  museum-web/tsconfig.json            # noUncheckedIndexedAccess: true (cf audit § 4.1)
  ```
- **Symptôme** : drift tsconfig entre les 3 apps. FE + Web ont `noUncheckedIndexedAccess: true` (cf audit S1 § 4.1), BE non. Conséquence : `array[i]` est typé `T` au lieu de `T | undefined`, masquant potentiellement des `TypeError: Cannot read property of undefined` runtime sur les optional indexes (top-K, pagination, validators arrays).
- **Pourquoi non résolu pré-launch** : 35-50 sites estimés à patcher (per audit Subagent A — array indexing : `topK[0]`, pagination, validators arrays dans `similarity.service.ts`, `chat-repository-queries.ts`, `chat.repository.typeorm.ts`, `jsonb-validator.ts`, `sources-validator.ts`). Effort 8-12h. Pré-launch J-16, on a P0 langfuse v3 EOL (T1.1) + P0-8 JWKS Zod casts (T1.2) plus prioritaires. ROI MEDIUM — le pattern existant `?.` est suffisamment défensif per audit 05-12.
- **Sprint d'origine** : audit 2026-05-16 (S1 § 4.2).
- **Effort estimé** : 8-12h.
- **Trigger** : toute mention `noUncheckedIndexedAccess` en code review, OU lecture audit 2026-05-16 § 4.2, OU détection d'un `TypeError: Cannot read property X of undefined` runtime sur un index array BE.
- **Deadline** : post-V1 sprint 1 (fenêtre 2026-06-01 → 2026-06-30).
- **Owner** : Tim (single-dev pre-launch).
- **Comment fermer** :
  1. Ajouter `"noUncheckedIndexedAccess": true` dans `museum-backend/tsconfig.json` (compilerOptions).
  2. Run `cd museum-backend && pnpm lint` (= `tsc --noEmit`) — recenser sites en erreur.
  3. Pour chaque site : ajouter guard `if (!item) continue;` OU narrowing destructuring `const [first] = arr; if (!first) ...` OU `as const` assertion si literal tuple.
  4. Vérifier `pnpm test` BE pass.
  5. Cocher TD-16 ici.
- **Note** : les sites cités dans l'audit (similarity.service.ts, chat-repository-queries.ts, chat.repository.typeorm.ts) ont 0 occurrence directe `[0]` au moment de la création du ticket — les patterns d'indexing sont probablement indirects (`.at()`, destructuring, `slice`). Comptage rigoureux à faire au moment de l'activation, l'estimation 35-50 est BE-wide (38 fichiers contiennent `[0]` au total).
```

### Décision d'insertion dans TECH_DEBT.md
- **Où** : entre la fin de TD-15 (ligne ~393) et le séparateur `---` + section `## Tech debts fermés` (ligne ~397).
- **ID** : `TD-16` (next séquentiel ; le YAML mentionne `TD-BE-noUncheckedIndexedAccess` mais c'est un slug descriptif — la convention numérique TD-N est utilisée par les 15 entrées existantes).
- **Priorité narrative** : open (P1 post-V1), pas P0 — l'activation elle-même est P1, seule la **documentation** est P0 per audit § 4.2.

---

## Tasks

Étapes ordonnées atomiques pour le green-code-agent :

- **T1.3.a** — Lire `docs/TECH_DEBT.md` (sections § Convention lignes 11-21, TD-15 fin ligne ~393, section `## Tech debts fermés` ligne ~397) pour confirmer le point d'insertion exact.
- **T1.3.b** — Vérifier qu'aucun TD-16 n'existe déjà dans le fichier (`grep -n "TD-16" docs/TECH_DEBT.md` → 0 résultat attendu).
- **T1.3.c** — Insérer le bloc template de la section Design § "Format proposé de l'entrée" juste après la fin de TD-15, avant le `---` qui précède `## Tech debts fermés`. Utiliser `Edit` tool (pas Write — fichier existant 400+ lignes).
- **T1.3.d** — Vérifier `grep -n "^### TD-" docs/TECH_DEBT.md` retourne bien TD-1..TD-16 sans gap.
- **T1.3.e** — Vérifier `grep -c "2026-05-16" docs/TECH_DEBT.md` ≥ +1 par rapport au baseline (entrée datée).
- **T1.3.f** — `git diff docs/TECH_DEBT.md` doit montrer +30 lignes environ, scope strict (zéro touch ailleurs).
- **T1.3.g** — Aucun test à run (doc-only change). Pre-commit hook doit passer (markdown linting si configuré).
- **T1.3.h** — Cocher la task T1.3 dans `docs/roadmap-night/audit-360-2026-05-16/tracking.md` si tracking existe.

---

## Open Questions

- **Q1** — Convention d'ID : le YAML mentionne `TD-BE-noUncheckedIndexedAccess`, mais le fichier existant utilise `TD-N` numérique (TD-1..TD-15). Le spec retient **TD-16** par cohérence avec les 15 entrées existantes. Si Tim préfère le slug descriptif, switcher en `TD-BE-noUncheckedIndexedAccess` est trivial (1 edit) — mais brise la séquence numérique attendue par les tools de tracking éventuels. **Recommandation : garder TD-16.**
- **Q2** — Le YAML AC mentionne "headings: Why, Trigger, Deadline, Estimated effort, Owner" — le format existant TECH_DEBT.md utilise plutôt `**Symptôme**`, `**Pourquoi non résolu**`, `**Effort estimé**`, `**Comment fermer**` (pas `Trigger`/`Deadline`/`Owner` explicites). Le spec ajoute **Trigger**, **Deadline**, **Owner** comme bullets supplémentaires (pas en heading level — bullet level pour cohérence visuelle avec les 15 autres entrées). Si le format JSON/structured-headings est attendu pour outillage downstream, escalade nécessaire — sinon le format bullet hybride est cohérent avec le tracker.
- **Q3** — Les 3 fichiers cités (similarity.service.ts, chat-repository-queries.ts, chat.repository.typeorm.ts) ont 0 occurrence directe `[0]` en spot-check. L'estimation 35-50 sites reste plausible BE-wide (38 fichiers concernés au total), mais les sites précis cités sont **présumés** par l'audit, pas vérifiés. La note de transparence ajoutée dans le template ci-dessus (§ "Note") explicite ce gap pour le futur green-code-agent qui fermera TD-16. **Pas besoin d'escalade — l'auditeur Subagent A a probablement profilé via un AST plus large que `grep [0]`.**
