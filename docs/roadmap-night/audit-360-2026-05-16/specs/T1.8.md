# T1.8 — Stale audit 05-12 entries : reflag MASTER.md

**Date :** 2026-05-16 · **App :** cross (docs) · **Priority :** P1 · **Effort :** 30min
**Source task :** `docs/roadmap-night/audit-360-2026-05-16/S1-types-libs.tasks.md` § T1.8
**Source audit :** `.claude/skills/team/team-reports/working/2026-05-16-audit-360/S1-types-libs-dryskiss.md` § 8

---

## Spec

### Intent
Mettre à jour `docs/audit-2026-05-12/MASTER.md` pour refléter l'état réel au 2026-05-16 des 5 entrées listées (P0-6, P0-7, P1-1, P2-6, P2-7). Hygiène doctrine — éviter qu'un futur agent re-flag ces findings comme blockers actifs.

### Acceptance Criteria (verbatim S1-types-libs.tasks.md)
MASTER.md historique mis à jour avec mentions de résolution datées 2026-05-16 :
- P0-6 ✅ RESOLVED 2026-05-14 (déjà fait dans MASTER.md, vérifier)
- P0-7 ⚠️ RESOLVED (api.ts:38-56 actuels = legitime ; soit fixed entre 05-12 et 05-13, soit audit 05-12 trompé sur la ligne)
- P1-1 ✅ RESOLVED (sentry-scrubber centralisé via @musaium/shared/observability)
- P2-6 ✅ RESOLVED (npm ls FE = empty, extraneous packages partis)
- P2-7 ❌ INCORRECT au moment de l'audit (Web a noUncheckedIndexedAccess depuis longtemps)

Update unique commit, message clair.

### Scope boundary
SEUL `docs/audit-2026-05-12/MASTER.md` est touché. Aucune autre file. Aucun code. Aucun script.

---

## Vérifications préalables (UFR-018)

### P0-6 — état dans MASTER.md déjà présent
Lecture `docs/audit-2026-05-12/MASTER.md:53` :
> `| **P0-6** ✅ RESOLVED 2026-05-14 | `museum-web/src/lib/admin-types.ts:UserRole` super_admin / RBAC silencieux. Re-vérifié au commit du fix : `admin-types.ts:86` contenait déjà `super_admin` au moment de l'audit ; le bug résiduel était dans `auth.tsx:RoleGuard.includes()` ... /team run `2026-05-14-admin-user-detail-page` corrige `RoleGuard` (`auth.tsx:272`) + ajoute test de régression Vitest ... Co-traité avec F4 Claim 1. ADR-050. | 03 | 30 min | Mauvaise vérification rôle admin Web |`

**Verdict** : la mention `✅ RESOLVED 2026-05-14` existe DÉJÀ dans la cell P0-6. Aucun ajout requis pour cette ligne — vérifier juste que la T1.8 ne re-double pas le marker. **NO-OP confirmé.**

### P0-7 — api.ts à la ligne citée
La task YAML cite `museum-web/src/lib/api.ts:38-56`. Lecture réelle du fichier :
- `api.ts:34` → `export function registerLogoutHandler(handler: () => void): void {`
- `api.ts:41` → `function readCsrfToken(): string | null {`
- `api.ts:48` → `const STATE_CHANGING_METHODS = new Set(['POST', 'PUT', 'PATCH', 'DELETE']);`
- `api.ts:50-56` → Refresh queue (`isRefreshing`, `failedQueue`, `processQueue`)

Aucun symbole `setTokens` / `clearTokens` / `getAccessToken` (zombies cités par l'audit 05-12) n'apparaît dans le fichier. Le code à ces lignes EST legitime (CSRF helper + auth refresh queue).

**Drift de numérotation** : audit 05-12 citait `38-56`, code 2026-05-16 a ces symbols à `34-48` — décalage de 4 lignes (probable suppression des zombies elle-même). Le verdict « api.ts:38-56 actuels = legitime » du S1-dryskiss reste vrai en substance, mais les lignes ont bougé.

**Verdict** : RESOLVED — les zombies cités par P0-7 ont été supprimés. Marquer ✅ avec mention du décalage de lignes.

### P1-1 — sentry-scrubber centralisé
Grep `sentry-scrubber` sur le repo confirme :
- **Source unique** : `packages/musaium-shared/src/observability/sentry-scrubber.ts` (+ `index.ts` barrel `observability/index.ts`).
- **3 apps re-exportent** depuis `@musaium/shared` (BE) ou `@musaium/shared/observability` (FE/Web) :
  - `museum-backend/src/shared/observability/sentry-scrubber.ts:16` → `import { ... } from '@musaium/shared';`
  - `museum-frontend/shared/observability/sentry-scrubber.ts:15` → `import { ... } from '@musaium/shared/observability';`
  - `museum-web/src/lib/sentry-scrubber.ts:16` → `import { ... } from '@musaium/shared/observability';`
- Chaque app-side fichier injecte le `hashEmail` runtime-agnostic local et re-exporte le reste — JSDoc verbatim : `« Logic — regex constants, traversal, URL/header/record scrubbing, breadcrumb dropping — lives in @musaium/shared/observability. »`
- Garde-fou : `scripts/sentinels/sentry-scrubber-parity.mjs` mentionné dans les JSDocs des 3 fichiers (parité gate).

**Verdict** : RESOLVED — la triplication PII redaction de l'audit 05-12 est éliminée. Marquer ✅.

### P2-6 — npm ls FE empty
Hors-scope agent (`npm ls` nécessite exécution). Note pour green-code : à vérifier par user (commande `npm ls --depth=0` dans `museum-frontend/` doit montrer 0 `extraneous`). La task YAML porte cette claim sans la re-vérifier ; le S1-dryskiss § 5.5 (« RÉSOLU ») la valide indirectement. Trust the prior audit, mais ne pas asserter sans run.

**Verdict** : RESOLVED selon S1-dryskiss § 5.5. Marquer ✅ avec mention « validé par S1 dryskiss 2026-05-16 § 5.5 ».

### P2-7 — Web tsconfig `noUncheckedIndexedAccess`
Lecture `museum-web/tsconfig.json:8` :
> `    "noUncheckedIndexedAccess": true,`

Le flag est ACTIVÉ (et `strict: true` à la ligne 7 aussi).

**Verdict** : P2-7 INCORRECT au moment de l'audit 05-12 (le flag est présent depuis au moins le 2026-05-16, possiblement plus tôt — pas vérifié via git blame, hors scope). Marquer ❌ INCORRECT.

---

## Design

### Format de l'update MASTER.md

Approche : **édition in-place des cellules existantes** dans les tableaux P0 / P1 / P2, ajout d'un prefix d'état `✅ RESOLVED 2026-05-16` ou `❌ INCORRECT (re-checked 2026-05-16)` dans la colonne `# Finding`, sans réécrire l'entièreté de la cellule.

Pattern de référence déjà présent dans le fichier : **P0-6** utilise `| **P0-6** ✅ RESOLVED 2026-05-14 | <description> ... | <source> | <effort> | <risk> |` — on suit le même pattern pour P0-7, P1-1, P2-6, P2-7.

Pour chaque entrée, on garde la description originale (preuve historique) + on append un suffix `→ resolved/incorrect 2026-05-16 : <preuve ≤2 phrases>`.

### Verdicts finaux + preuves

| Entry | Verdict | Preuve à intégrer dans MASTER.md |
|---|---|---|
| P0-6 | NO-OP | Déjà marqué `✅ RESOLVED 2026-05-14` ligne 53. Aucune modification. |
| P0-7 | ✅ RESOLVED 2026-05-16 | « zombie exports supprimés ; api.ts actuel = `registerLogoutHandler` (l.34) + `readCsrfToken` (l.41) + `STATE_CHANGING_METHODS` (l.48), pas de `setTokens`/`clearTokens`/`getAccessToken`. Lignes ont bougé (38-56 → 34-48). » |
| P1-1 | ✅ RESOLVED 2026-05-16 | « sentry-scrubber centralisé dans `packages/musaium-shared/src/observability/sentry-scrubber.ts` ; BE/FE/Web re-exportent. Parité gate `scripts/sentinels/sentry-scrubber-parity.mjs`. » |
| P2-6 | ✅ RESOLVED 2026-05-16 | « validé par S1 dryskiss § 5.5 (`npm ls` empty). User doit re-confirmer au green-code-time. » |
| P2-7 | ❌ INCORRECT (re-checked 2026-05-16) | « `museum-web/tsconfig.json:8` = `noUncheckedIndexedAccess: true`. Audit 05-12 erroné. » |

### Niveau heading
Pas de nouveau heading. Édition cellulaire dans les tables existantes (P0, P1, P2). Le fichier reste un rapport historique 2026-05-12 — on annote, on ne ré-écrit pas.

---

## Tasks (atomic, ordered)

### T1.8.a — Vérifier P0-6 NO-OP
- Re-lire MASTER.md:53 pour confirmer que la mention `✅ RESOLVED 2026-05-14` persiste (depuis cette spec).
- Si présente : skip l'édition P0-6. Si absente (régression docs entre cette spec et l'exécution) : restaurer le marker.

### T1.8.b — Marquer P0-7 RESOLVED
- Édit `docs/audit-2026-05-12/MASTER.md`, table P0, ligne P0-7.
- Remplacer le label `| **P0-7** |` par `| **P0-7** ✅ RESOLVED 2026-05-16 |` et appendre dans la cellule Finding après le texte existant :
  > `→ resolved 2026-05-16 : zombie exports supprimés ; ` `api.ts` actuel contient `registerLogoutHandler` (l.34) + `readCsrfToken` (l.41) + `STATE_CHANGING_METHODS` (l.48), aucun `setTokens`/`clearTokens`/`getAccessToken`. Lignes ont migré (38-56 → 34-48 — décalage de la suppression des zombies eux-mêmes).`

### T1.8.c — Marquer P1-1 RESOLVED
- Édit MASTER.md table P1, ligne P1-1.
- Remplacer le label `| P1-1 |` par `| P1-1 ✅ RESOLVED 2026-05-16 |` et appendre dans la cellule Finding :
  > `→ resolved 2026-05-16 : sentry-scrubber centralisé dans ` `packages/musaium-shared/src/observability/sentry-scrubber.ts` `. BE/FE/Web re-exportent via ` `@musaium/shared` ` / ` `@musaium/shared/observability` `. Parité gardée par ` `scripts/sentinels/sentry-scrubber-parity.mjs` `.`

### T1.8.d — Marquer P2-6 RESOLVED
- Édit MASTER.md table P2, ligne P2-6.
- Remplacer le label `| P2-6 |` par `| P2-6 ✅ RESOLVED 2026-05-16 |` et appendre dans la cellule Finding :
  > `→ resolved 2026-05-16 : validé par S1 dryskiss § 5.5 (` `npm ls` ` FE = empty, extraneous partis). À re-confirmer par user au green-code-time.`

### T1.8.e — Marquer P2-7 INCORRECT
- Édit MASTER.md table P2, ligne P2-7.
- Remplacer le label `| P2-7 |` par `| P2-7 ❌ INCORRECT (re-checked 2026-05-16) |` et appendre dans la cellule Finding :
  > `→ re-checked 2026-05-16 : ` `museum-web/tsconfig.json:8` ` = ` `"noUncheckedIndexedAccess": true` `. L'audit 05-12 a été erroné sur ce point — Web l'a depuis au moins 2026-05-16 (git blame pas exécuté, hors scope T1.8).`

### T1.8.f — Commit unique
- `git status` pour confirmer scope (seul `docs/audit-2026-05-12/MASTER.md` modifié).
- Commit message :
  ```
  docs(audit-05-12): reflag 5 stale entries (P0-6/7, P1-1, P2-6/7)

  - P0-6 : déjà marqué RESOLVED 2026-05-14 (no-op confirmé)
  - P0-7 : RESOLVED 2026-05-16 (zombie exports supprimés, api.ts:34-48 legitime)
  - P1-1 : RESOLVED 2026-05-16 (sentry-scrubber centralisé via @musaium/shared)
  - P2-6 : RESOLVED 2026-05-16 (per S1 dryskiss § 5.5)
  - P2-7 : INCORRECT — Web tsconfig l'a (noUncheckedIndexedAccess: true)

  Source vérifications : docs/roadmap-night/audit-360-2026-05-16/specs/T1.8.md
  Tâche : S1-types-libs.tasks.md T1.8
  ```
- Hooks pre-commit doivent passer (docs-only, low risk).

---

## Open Questions

### Q1 — P0-7 line drift : signaler ou ignorer ?
La task YAML dit `api.ts:38-56 actuels = legitime`. Vérification réelle : `api.ts:34-48`. Décalage de 4 lignes (lié probablement à la suppression des zombies eux-mêmes). **Non bloquant** — la nature du code (CSRF / refresh queue) confirme la legitimité. Spec mentionne le décalage pour traçabilité ; pas de TASK YAML DRIFT signalé (le verdict de fond est correct).

### Q2 — P2-6 sans re-vérification commande
La spec ne re-run pas `npm ls` dans `museum-frontend/`. Trust S1 dryskiss § 5.5. Si le green-code-agent veut être strict UFR-018, il peut ajouter `cd museum-frontend && npm ls --depth=0 | grep extraneous` au commit message comme preuve. Pas obligatoire pour T1.8 — c'est de la doc, pas du code.

### Q3 — P2-7 git blame profondeur
Le S1 dryskiss § 8 dit « depuis longtemps » sans dater l'introduction. T1.8 ne demande pas de git blame — la spec accepte le « ❌ INCORRECT au moment de l'audit » sans pousser plus loin. Si tracking historique souhaité (« incorrect depuis quand exactement ? »), c'est hors scope T1.8 — créer une T1.8.bis ou laisser.
