node_modules
dist
coverage
.git
.gitignore
.DS_Store
.env
.env.*
npm-debug.log

# C3 (2026-05) — local ONNX bundle. Fetched inside the prod build stage by
# scripts/fetch-models.sh; never shipped from the host context. Excluded here
# so a dev-machine `models/` directory does not bloat the build context.
models/

# Test & dev files
tests/
*.test.ts
jest.config.ts

# Documentation
docs/
*.md
!README.md

# Scripts (only keep those needed at runtime: seed scripts for deploy)
scripts/*
!scripts/seed-museums.ts
!scripts/seed-knowledge.ts
!scripts/seed-smoke-account.ts
# C3 (2026-05) — fetch-models.sh runs in the build stage of Dockerfile.prod.
!scripts/fetch-models.sh

# Docker configs (avoid recursive context)
docker-compose*.yml
deploy/

# Editor & CI
.vscode/
.claude/
.github/
