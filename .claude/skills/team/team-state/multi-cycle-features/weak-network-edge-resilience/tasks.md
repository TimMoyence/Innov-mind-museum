Anchors confirmed: DataModeProvider resolveDataMode at the verified shape; httpClient timeout 15000 (line 168 is `axios.create({`, 172 is `timeout: 15000`); DEFAULT_BACKOFF_MS [500,2000,8000] at retry.ts:34; env.jsonBodyLimit is string `'1mb'`; CORS allowedHeaders lacks Content-Encoding and Idempotency-Key; peek() drops id; dataModeStore at features/settings. All cluster corrections are validated. Producing the synthesized tasks.md.

# tasks.md — Wave 1: weak-network-edge-resilience

**Feature slug:** `weak-network-edge-resilience` · **Wave:** W1
**Spec (authoritative, locked):** `/Users/Tim/Desktop/all/dev/Pro/InnovMind/docs/superpowers/specs/2026-06-01-weak-network-resilience-and-test-track-design.md`
**Locked user decisions:** D1 adaptive image (150KB WebP uploaded+server-persisted / 250–400KB local enhancement) · D2 backend Idempotency-Key dedup · D4 durable carnet re-download + dedicated capped local cache. (D3 fault-injection OFF-in-prod-no-escape-hatch = **W2**.)
**Discipline:** UFR-022 red→green (every `kind:test` precedes the `kind:code` it unlocks; tests FROZEN once written, byte-for-byte). UFR-013 honesty (anchors below are file-verified, not spec-trusted). DRY factories (`docs/TEST_FACTORIES.md`). Path aliases `@modules/@shared/@data` (BE), `@/` (FE). No `eslint-disable`. Hexagonal BE, feature-driven FE.

---

## Anchor corrections

These supersede the spec's "Verification ledger" line numbers. The green phase MUST use these, not the spec.

1. **`resolveDataMode` is NOT in `shared/infrastructure/connectivity/`.** It lives at `museum-frontend/features/chat/application/DataModeProvider.tsx:44` (signature verified: `(preference, netInfo:{isConnected:boolean|null; type:string; details:{isConnectionExpensive?:boolean; cellularGeneration?:string|null}|null})`). Branches verified EXACT: `:52` `if (preference==='low') return 'low'`; `:56` `if (netInfo.isConnected===false) return 'low'`; `:58–63` cellular 2g/3g → `'low'`. `isConnectionExpensive` is read from **`netInfo.details.isConnectionExpensive`** (nested, not top-level). `isOnline.ts` IS in `shared/infrastructure/connectivity/` — that is where `networkProfiles.ts` co-locates. Self-tests import `resolveDataMode` from `@/features/chat/application/DataModeProvider`.

2. **DataMode preference store is at `museum-frontend/features/settings/dataModeStore.ts`** (verified), NOT a connectivity path. `useDataModePreferenceStore = create(persist(...))` at `:30`; default `preference:'auto'` (`:33`); `setPreference:(p)=>set({preference:p})` (`:35`); persist name `musaium.dataMode.preference`, `partialize {preference}`. `DataModePreference = 'auto'|'low'|'normal'`. `useDataMode()` hook returns `{preference,resolved,isLowData,setPreference}` at `DataModeProvider.tsx:101`. The dev route imports from `@/features/settings/dataModeStore`.

3. **axios timeout is at `httpClient.ts:172` (`timeout: 15000`), NOT `:168`** (`:168` is `axios.create({`). Use `AXIOS_TIMEOUT_MS=15000` sourced/asserted from `:172`, never hardcoded blind. `maxContentLength/maxBodyLength` = 10 MiB. `X-Data-Mode` set from `getCurrentDataMode()` at `httpClient.ts:192–193` (the single resolved-DataMode seam to reuse for FE gzip gating).

4. **The `[500,2000,8000]` schedule is `DEFAULT_BACKOFF_MS` at `retry.ts:34`, consumed by `runWithRetry`/`useOfflineSync` — NOT the axios interceptor.** The axios interceptor (`httpClient.ts:314–346`) uses a DIFFERENT schedule (`150*(retryCount+1)` non-429, exponential for 429, max 2 retries). M5 (`W1-L1-14-M5-TEST`) MUST bind `runWithRetry`/`retry.ts`, not the interceptor. `isRetryableError` treats `Network`/`Timeout`/`RateLimited` AppError kinds as retryable (`retry.ts:19–20`).

5. **Idempotency-Key forwarding does NOT exist anywhere today (net-new, 3-file change, NOT a one-line add).** `PostMessageParams` (`send.ts:20–37`) has no key field; `postMessage` (`send.ts:81`) sets only `X-Data-Mode`; `useOfflineSync.ts:59–69` flush passes no id; and crucially `peek()`'s return type (`useOfflineSync.ts:21`, verified `{ sessionId; text?; imageUri? }`) **DROPS `id`** so `queued.id` isn't even reachable. `QueuedMessage.id` exists (`offlineQueue.ts:1–8`, generated `offline-<ts>-<rand>` at enqueue). M4/D2 must widen `peek()`/`UseOfflineSyncParams` AND thread id into `postMessage` as `Idempotency-Key`.

6. **`env.jsonBodyLimit` is the STRING `'1mb'` (`env.ts:66`, typed string), not a number.** The zip-bomb cap MUST parse it to bytes (`'1mb'` → 1048576) before comparing. `express.json({ limit: env.jsonBodyLimit })` at `app.ts:177`; `compression()` at `app.ts:163–170`; `res.setTimeout` setter at `app.ts:172–175`; `applyGlobalMiddleware(app)` at `:125`; `errorHandler` terminal at `:282`. Decompression mounts after compression and STRICTLY before `express.json`.

7. **CORS `allowedHeaders` (`app.ts:144–151`, verified) contains `Content-Type/Authorization/X-Request-Id/X-Data-Mode/Accept-Language/sentry-trace/baggage`** — it does NOT contain `Content-Encoding` (gzip cluster adds it) nor `Idempotency-Key` (dedup cluster adds it). **Cross-cluster merge hazard:** both edit this same array block — coordinate so they don't conflict-merge.

8. **Parity-sentinel template:** `scripts/sentinels/cache-key-parity.mjs` is a thin jest-runner wrapper, NOT a structural sha256 sentinel; the spec's "model on cache-key-parity" is wrong. Model on `scripts/sentinels/sentry-scrubber-parity.mjs` (uses `node:crypto createHash`) + the pure-structural style of `museum-backend/scripts/sentinels/security-headers-invariants.mjs`. (Note: `scripts/sentinels/cache-key-parity.mjs` DOES exist and IS wired in `sentinel-mirror.yml:111` — the mirror-wiring pattern is correct; only its structural model was misattributed.)

9. **`error.middleware.ts:54–72`** `normalizeError` builds `AppError({statusCode:413, code:'PAYLOAD_TOO_LARGE'})`; envelope `{error:{code,message,requestId,details?}}`. `AppError` (`app.error.ts:1–17`) constructs `413 PAYLOAD_TOO_LARGE` directly.

10. **D4 BE half is ALREADY BUILT — spec risk R9 ("server persistence may not exist") is FALSE.** Recognition upload is durably persisted via `imageStorage.save()` (`image-processing.service.ts:97`, S3 or local stub, user-scoped key `chat-images/user-<id>/YYYY/MM/<uuid>.<ext>`, GDPR-deletable). TWO authenticated re-download paths exist and are in `openapi.json`: `POST /api/chat/messages/{messageId}/image-url` (ownership-checked via `ensureMessageAccess`, `chat-media.route.ts:277–282`) and `GET /api/chat/messages/{messageId}/image` (signed-HMAC-token auth, intentionally session-ownership-bypassing, TTL-bound, `:294`). BE W1 = **verify/regression-lock the contract**, not build-from-scratch. Load-bearing: post-wipe the FE MUST re-mint a fresh signed URL via the ownership-checked `image-url` endpoint, never replay a stale signed GET.

11. **`offlineImageStorage.ts` (`:1–55`) is a DUMB persist helper** (copy temp→`documentDirectory/offline-images/`), zero cap/index/LRU/maxAge/size-accounting — a full rewrite-in-place. Its 3 exported fns are consumed by `useOfflineQueue.ts:9–12` (resync path) — keep signatures stable; run `gitnexus_impact` before editing. TTS cache precedent at `useTextToSpeech.ts:49–79` uses `cacheDirectory/tts/`; the image cache MUST use a DEDICATED dir/quota (`documentDirectory/offline-images/`), not share TTS quota.

12. **`compressionDecision.pure.ts` does NOT exist; `optimizeImageForUpload(uri)` is single-arg, returns a `string`, hardcodes `SaveFormat.JPEG` @1600px target `2_700_000` (`imageUploadOptimization.ts:6–8,72–75,56`).** 9 existing frozen tests call it single-arg and mock `SaveFormat` WITHOUT `WEBP`. The two-output D1 requirement CANNOT reuse the string-returning signature — add a sibling adaptive function/overload; keep the legacy export's string return + JPEG/1600px default so the 9 frozen tests stay green. `SaveFormat.WEBP='webp'` is statically present (expo-image-manipulator@55.0.15, in validators allowedFormats); native-encoder fidelity per-platform remains a RUNTIME check. WebP wire-MIME works ONLY because `_internals.ts:29–31` maps `.webp`→`image/webp` from the URI extension — load-bearing, the optimized URI must end in `.webp`.

13. **`shared/testing/`, `mulberry32`, `FakeClock`, `makeQueuedMessage` factory, and the `no-restricted-imports` ESLint boundary rule ALL do not exist (net-new).** `pako` is NOT a museum-frontend dep and there is NO `lib-docs/pako/` — doc-cache must fetch pako docs in red/green. `lib-docs/@react-native-community/netinfo` (11.5.2), `express`, `ioredis`, `axios`, `cors`, `react`, `pg`, `expo-image-manipulator/expo-image/expo-image-picker/expo-file-system`, `js-sha256` all present.

14. **`OfflineQueue.getAll()` returns `this.snapshot`, populated only by `notify()`, NOT by `hydrate()`** — `getAll()` after a hydrate-with-data returns `[]` (snapshot-init gap, `offlineQueue.ts:30/162`). M3 must pin this regression. `offlineQueue.ts` IS in `coveragePathIgnorePatterns` (`jest.config.js:46`); M3 removes it; branch threshold (`jest.config.js:62` = 78) must hold (never lower it).

15. **`configureGuardrailFriction({cache})` is called at `chat-module.ts:719`** with the shared `CacheService` in scope — `configureIdempotency({cache})` wires identically. `CacheService.setNx(key,value,ttlSeconds):Promise<boolean>` (`cache.port.ts:26`) is the atomic put-if-absent reserve primitive. Dedup store mirrors `guardrail-friction.store.ts` (dual in-process + Redis-via-CacheService, `__setStoreForTest`/`__setNowForTest` seams). Decision: Redis-via-CacheService primary + in-process fallback, **FAIL-OPEN** on store outage (never 500 the chat hot path nor block a first send). NOT a `@musaium/shared` export (avoids `file:`-package churn).

---

## Dependency order

1. **Registry keystone first** (`W1-REG-01..13`) — frozen `networkProfiles.ts` + BE vendored copy + parity sentinel. Everything else that references profiles consumes it.
2. **L1 harness + factories** (`W1-L1-*`) — depends on the registry landing; provides `shared/testing/` helpers (A/B/C), `makeQueuedMessage`, M1–M7 tests, ESLint boundary, coverage-ignore removal.
3. **D2 Idempotency** (`W1-IDEM-*`) — FE↔BE pair; the FE M4 test (`W1-L1-12`/`W1-IDEM-05`) and code (`W1-L1-13`/`W1-IDEM-06`) are the SAME deliverable merged once (see Tasks). Widening `peek()` is a shared-contract change → full-suite verify.
4. **D1 image compression** (`W1-D1FE-*`) — `compressionDecision.pure.ts` (owned here; M6 in L1 is the consuming unit test) → adaptive `optimizeImageForUpload` → picker wiring → runtime WebP gate. Emits the local derivative consumed by D4.
5. **D4 carnet/cache** (`W1-D4-*`) — BE contract-lock + FE capped cache + resolver; consumes D1's derivative.
6. **Gzip** (`W1-GZIP-*`) — BE decompression middleware + FE conditional gzip; independent except for the shared CORS array edit.
7. **Dev route** (`W1-DEV-*`) — independent; deterministic low-data trigger.

Within every cluster: each `kind:test` (RED, frozen) immediately precedes the `kind:code` (GREEN) that makes it pass. FE/BE pairs are grouped. Final cumulative full-suite verify scopes BROADER than per-cluster patterns.

---

## Tasks

### Cluster A — Network Profile Registry (keystone) + parity sentinel

- [x] **W1-REG-01** (test) RED: registry shape + frozen `NETWORK_PROFILES` contract test — files: `museum-frontend/__tests__/shared/infrastructure/connectivity/networkProfiles.test.ts` — acceptance: WHEN the registry module is imported THE SYSTEM SHALL expose a frozen `NETWORK_PROFILES` record with exactly 6 keys (offline/2g/edge/3g-lossy/flapping/normal) carrying the ratified §Canonical numbers (offline lat0/jit0/down0/up0/loss1.0/none/null/low; 2g 350/150/100/40/0.02/cellular/2g/low; edge 200/120/200/90/0.01/cellular/2g/low; 3g-lossy 120/80/700/300/0.08/cellular/3g/low; normal 25/10/20000/8000/0.0/wifi/null/normal), `Object.isFrozen` true at record AND per-profile level, ONLY flapping carries `dutyCycle {onlineMs:5000,offlineMs:3000,baseProfile:'3g-lossy'}`, and the module imports neither `react` nor `@react-native-community/netinfo` (source-grep assertion) — libDocs: @react-native-community/netinfo — depends: —
- [x] **W1-REG-02** (code) GREEN: implement `networkProfiles.ts` (TS shape, frozen registry, typed mapper stubs) — files: `museum-frontend/shared/infrastructure/connectivity/networkProfiles.ts` — acceptance: WHEN W1-REG-01 runs THE SYSTEM SHALL pass; define `NetinfoType/CellularGeneration/NetworkProfileName/DutyCycle/NetworkProfile/FlapTick` per §TS-shape; `Object.freeze` the record and each profile; PURE module owning plain string-literal enums (no React/NetInfo import); co-located with `isOnline.ts` — libDocs: @react-native-community/netinfo — depends: W1-REG-01
- [x] **W1-REG-03** (test) RED: registry self-test feeding REAL `resolveDataMode` per profile — files: `museum-frontend/__tests__/shared/infrastructure/connectivity/networkProfiles.selftest.test.ts` — acceptance: WHEN each profile is converted to a netInfo snapshot `{isConnected,type,details:{isConnectionExpensive:false,cellularGeneration}}` and passed to the REAL `resolveDataMode('auto',snapshot)` from `@/features/chat/application/DataModeProvider` THE SYSTEM SHALL return `profile.expectedDataMode` for all non-flapping; for flapping it SHALL assert BOTH the OFFLINE-window (`isConnected:false`→`'low'` via `:56`) AND the ONLINE 3g-lossy window (→`'low'` via `:58–63`). `it.each` over `Object.values`. `isConnectionExpensive` nested under `details`. Mock netinfo enums exactly as `DataModeProvider.test.tsx`. SHALL FAIL until snapshot-mapper helper exported — libDocs: @react-native-community/netinfo — depends: W1-REG-02
- [x] **W1-REG-04** (code) GREEN: export `toNetInfoSnapshot(p, opts?)` helper — files: `museum-frontend/shared/infrastructure/connectivity/networkProfiles.ts` — acceptance: WHEN W1-REG-03 runs THE SYSTEM SHALL pass; pure exported `toNetInfoSnapshot(p, {online?})` returning `{isConnected, type, details:{isConnectionExpensive:false, cellularGeneration}}` matching the verified `resolveDataMode` netInfo param; flapping `online:false`→`isConnected:false`. Consumed by tests + L1 harness only (no `app/` import) — libDocs: @react-native-community/netinfo — depends: W1-REG-03
- [x] **W1-REG-05** (test) RED: `flapScheduleAt` duty-cycle phase math — files: `museum-frontend/__tests__/shared/infrastructure/connectivity/flapSchedule.test.ts` — acceptance: WHEN `flapScheduleAt(NETWORK_PROFILES.flapping, elapsedMs)` is called THE SYSTEM SHALL return `{online:true, baseProfile:NETWORK_PROFILES['3g-lossy']}` for `[0,5000)` and `[8000,13000)`, `{online:false,…}` for `[5000,8000)`; phase = `elapsedMs % 8000`; assert boundary exactness at 0/4999/5000/7999/8000/13000; SHALL throw on a non-flapping profile. SHALL FAIL until implemented — libDocs: — depends: W1-REG-02
- [x] **W1-REG-06** (code) GREEN: implement `flapScheduleAt` — files: `museum-frontend/shared/infrastructure/connectivity/networkProfiles.ts` — acceptance: WHEN W1-REG-05 runs THE SYSTEM SHALL pass; `phase = elapsedMs % (onlineMs+offlineMs)`, `online = phase < onlineMs`; throws typed Error if `dutyCycle` undefined; pure/deterministic (elapsedMs injected, no Date.now) — libDocs: — depends: W1-REG-05
- [x] **W1-REG-07** (test) RED: three mappers (`toFetchMockShape`/`toMiddlewareDescriptor`/`toToxics`) pure unit tests — files: `museum-frontend/__tests__/shared/infrastructure/connectivity/networkProfileMappers.test.ts` — acceptance: WHEN each mapper is applied to each profile THE SYSTEM SHALL: (a) `toFetchMockShape(edge)`→`{preResponseDelayMs:200, failProbability:0.01, msPerKbitDown/Up derived from 200/90kbps, netinfo=toNetInfoSnapshot(edge), forcedDataModePreference:'low'}`, `toFetchMockShape(normal).forcedDataModePreference` undefined/`'normal'`; (b) `toMiddlewareDescriptor(2g)`→`{delayMs:350,jitterMs:150,failProbability:0.02,sseChunkDelayMs derived,ingressKbps:40}`; (c) `toToxics(edge)` bandwidth toxic rate = `bwUpKbps/8 = 11.25 KB/s` — assert kbps→KB/s conversion lives ONLY here (the other two mappers emit NO KB/s field), offline→blocking/timeout toxic (bw 0). SHALL FAIL until implemented — libDocs: — depends: W1-REG-02, W1-REG-04
- [x] **W1-REG-08** (code) GREEN: implement the three pure mappers — files: `museum-frontend/shared/infrastructure/connectivity/networkProfiles.ts` — acceptance: WHEN W1-REG-07 runs THE SYSTEM SHALL pass; `toFetchMockShape`→L1 Jest shape, `toMiddlewareDescriptor`→L2 descriptor, `toToxics`→`ToxiproxyToxic[]` with the SINGLE kbps/8→KB/s site; all pure; offline→blocking; no mapper recomputes another's knob (DRY); export `ToxiproxyToxic` type — libDocs: — depends: W1-REG-07
- [x] **W1-REG-09** (test) RED: BE↔FE byte-identical vendored copy parity test — files: `museum-backend/tests/contract/network-profiles-parity.test.ts` — acceptance: WHEN the data region of `museum-frontend/shared/infrastructure/connectivity/networkProfiles.ts` and `museum-backend/src/shared/net-shaping/networkProfiles.ts` are sha256-hashed (`node:fs`+`node:crypto`, repo-root-relative) THE SYSTEM SHALL assert the two hashes EQUAL. SHALL FAIL (BE copy absent). DRY: hash bytes, no inline literals — libDocs: js-sha256 — depends: W1-REG-08
- [x] **W1-REG-10** (code) GREEN: vendor byte-identical BE copy — files: `museum-backend/src/shared/net-shaping/networkProfiles.ts` — acceptance: WHEN W1-REG-09 runs THE SYSTEM SHALL pass; the NETWORK_PROFILES literal + TS shape BYTE-IDENTICAL to FE; self-contained (no FE import), importable via `@shared/net-shaping/networkProfiles`; creates the new `src/shared/net-shaping/` dir; NOT a `@musaium/shared` export — libDocs: — depends: W1-REG-09
- [x] **W1-REG-11** (test) RED: parity sentinel self-test (spawn the script) — files: `museum-backend/tests/unit/net-shaping/net-profiles-parity-sentinel.test.ts` — acceptance: WHEN the sentinel runs via `spawnSync('node',[sentinelPath])` against the real repo THE SYSTEM SHALL exit 0 when byte-identical; SHALL exit 1 + non-empty stderr when a temp-mutated BE copy (pointed via `NET_PROFILES_BE_PATH`) diverges. SHALL FAIL until the script exists — libDocs: — depends: W1-REG-10
- [x] **W1-REG-12** (code) GREEN: pure-Node sha256 parity sentinel — files: `scripts/sentinels/net-profiles-parity.mjs`, `museum-backend/package.json` — acceptance: WHEN `scripts/sentinels/net-profiles-parity.mjs` runs THE SYSTEM SHALL read both registry files (BE path overridable via `NET_PROFILES_BE_PATH`), sha256 each via `node:crypto createHash`, print PASS + exit 0 on match / diff hint + exit 1 on mismatch — modeled on `sentry-scrubber-parity.mjs` + structural style of `security-headers-invariants.mjs` (NOT `cache-key-parity.mjs`); add npm script `sentinel:net-profiles-parity`; no eslint-disable. (W2's `net-fault-prod-guard.mjs` is out of scope — keep names distinct.) — libDocs: — depends: W1-REG-11
- [x] **W1-REG-13** (config) Wire parity sentinel into `sentinel-mirror.yml` (anti-bypass UFR-020) — files: `.github/workflows/sentinel-mirror.yml` — acceptance: WHEN sentinel-mirror runs in CI THE SYSTEM SHALL execute `node scripts/sentinels/net-profiles-parity.mjs` as a new named step (mirroring existing step style ~lines 111/120/167) so FE↔BE registry drift fails the PR server-side; rides the existing sentinel-mirror job (no new required-check NAME). Pure-config — exempt from red/green pairing — libDocs: — depends: W1-REG-12

### Cluster B — L1 harness helpers + M1–M7 + ESLint boundary + coverage removal

- [x] **W1-L1-00-REG-DEP** (config) Consume Network Profile Registry (cross-cluster hard dep) — files: `museum-frontend/shared/infrastructure/connectivity/networkProfiles.ts` — acceptance: GIVEN W1-REG landed `networkProfiles.ts` THEN this cluster imports it from `@/shared/infrastructure/connectivity/networkProfiles`; WHEN absent THE SYSTEM SHALL block (no duplication) — libDocs: — depends: W1-REG-02, W1-REG-08
- [x] **W1-L1-01-FACT** (code) Add `makeQueuedMessage` / `makeNetInfoSnapshot` DRY factories — files: `museum-frontend/__tests__/helpers/factories/offline-queue.factories.ts`, `museum-frontend/__tests__/helpers/factories/index.ts` — acceptance: WHEN a test needs a `QueuedMessage`/NetInfo snapshot THE SYSTEM SHALL provide `makeQueuedMessage(overrides?)` (id/sessionId/text/imageUri/createdAt/retryCount sane defaults) and `makeNetInfoSnapshot(overrides?)`, satisfying `musaium-test-discipline/no-inline-test-entities`; honors `docs/TEST_FACTORIES.md` — libDocs: @react-native-community/netinfo — depends: —
- [x] **W1-L1-02-HELP-A-TEST** (test) RED: helper A `netInfoFromProfile` — files: `museum-frontend/shared/testing/__tests__/netInfoFromProfile.test.ts` — acceptance: WHEN `netInfoFromProfile(p)` is called per profile THE SYSTEM SHALL return `{type,isConnected,isInternetReachable,details:{isConnectionExpensive,cellularGeneration}}` matching `p.netinfoType/p.cellularGeneration` (offline→`{type:'none',isConnected:false}`; 2g/edge→cellular/2g; normal→wifi). FAILS (module absent) — libDocs: @react-native-community/netinfo — depends: W1-L1-00-REG-DEP, W1-L1-01-FACT
- [x] **W1-L1-03-HELP-A-CODE** (code) GREEN: implement helper A — files: `museum-frontend/shared/testing/netInfoFromProfile.ts` — acceptance: WHEN given a `NetworkProfile` THE SYSTEM SHALL emit the exact NetInfo state shape `resolveDataMode`/`isOnline` consume (PATTERNS.md:126 `isInternetReachable` nullable; :142 boolean|null never cast); pure, no React/NetInfo runtime import — libDocs: @react-native-community/netinfo — depends: W1-L1-02-HELP-A-TEST
- [x] **W1-L1-04-HELP-B-TEST** (test) RED: helper B `withNetworkSim` (mulberry32 + fake timers) — files: `museum-frontend/shared/testing/__tests__/withNetworkSim.test.ts` — acceptance: WHEN `withNetworkSim(realFn,{profile,clock,rng})` wraps a call under 'edge' THE SYSTEM SHALL (a) advance the injected FakeClock by profile latency before resolving, (b) drop calls deterministically per seeded mulberry32 at `profile.lossPct` (same seed→same sequence, NEVER `Math.random`), (c) map injected delay > `AXIOS_TIMEOUT_MS(15000, httpClient.ts:172)` to a Timeout `AppError` with `isRetryableError===true`. `jest.useFakeTimers`. FAILS — libDocs: — depends: W1-L1-00-REG-DEP
- [x] **W1-L1-05-HELP-B-CODE** (code) GREEN: implement helper B + mulberry32 + FakeClock — files: `museum-frontend/shared/testing/withNetworkSim.ts`, `museum-frontend/shared/testing/mulberry32.ts`, `museum-frontend/shared/testing/fakeClock.ts` — acceptance: WHEN wrapping `realFn` THE SYSTEM SHALL apply deterministic latency (FakeClock) + seeded loss (mulberry32) + the >15000ms→Timeout AppError mapping; the 15000 constant sourced/asserted against `httpClient.ts:172`, not blind — libDocs: — depends: W1-L1-04-HELP-B-TEST
- [x] **W1-L1-06-HELP-C-TEST** (test) RED: helper C `paceTokens` — files: `museum-frontend/shared/testing/__tests__/paceTokens.test.ts` — acceptance: WHEN `paceTokens(text,profile,onToken,clock)` emits a profile-paced stream THE SYSTEM SHALL deliver tokens whose concatenation equals input and whose inter-token gaps derive from `profile.bwDownKbps` (deterministic via FakeClock). NG2: sync-fallback emitter for `sendMessageStreaming.ts:78` onToken accumulation, NOT a real wire stream. FAILS — libDocs: — depends: W1-L1-00-REG-DEP, W1-L1-05-HELP-B-CODE
- [x] **W1-L1-07-HELP-C-CODE** (code) GREEN: implement helper C — files: `museum-frontend/shared/testing/paceTokens.ts` — acceptance: WHEN called THE SYSTEM SHALL produce a profile-paced `onToken` emitter using FakeClock — libDocs: — depends: W1-L1-06-HELP-C-TEST
- [x] **W1-L1-08-M1-TEST** (test) RED: M1 registry-parameterised `resolveDataMode` self-test — files: `museum-frontend/__tests__/features/chat/resolveDataMode.registry.test.ts` — acceptance: WHEN `it.each` over `NETWORK_PROFILES` feeds `netInfoFromProfile(p)` into the REAL `resolveDataMode('auto',snapshot)` THE SYSTEM SHALL assert result === `p.expectedDataMode` for every profile (offline/2g/edge/3g-lossy/flapping→'low'; normal→'normal'); if the rule changes this goes red (registry can never silently lie). FAILS until helper A + registry exist — libDocs: @react-native-community/netinfo — depends: W1-L1-03-HELP-A-CODE *(coordinate with W1-REG-03 — same anti-drift invariant from the harness side; both kept: REG-03 lives in the registry suite, M1 in the chat suite. Do not merge — they prove the same property from two test homes by design.)*
- [x] **W1-L1-09-M2-TEST** (test) RED: M2 enqueue-on-disconnect seam — files: `museum-frontend/__tests__/features/chat/enqueueOnDisconnect.seam.test.ts` — acceptance: WHEN `pickSendStrategy(attempt,{isOffline:true,…})` (`chatSessionStrategies.pure.ts:43`) returns 'offline' AND `sendMessageOffline(attempt,context)` runs THE SYSTEM SHALL call `context.enqueue` exactly once with `{sessionId,text,imageUri}`, append one optimistic message keyed on `queued.id`, return true; WHEN `enqueue` resolves null (full) THE SYSTEM SHALL return false and append NO optimistic message. Uses `makeQueuedMessage` — libDocs: — depends: W1-L1-01-FACT
- [x] **W1-L1-10-M3-TEST** (test) RED: M3 OfflineQueue full/FIFO/maxAge/prune/corrupt-JSON/getAll-after-hydrate — files: `museum-frontend/__tests__/features/chat/offlineQueue.test.ts` — acceptance: WHEN OfflineQueue is exercised THE SYSTEM SHALL prove enqueue returns null at `maxQueueSize` (`:88–91`); dequeue FIFO (`:106`); hydrate drops entries older than `maxAgeMs` + fires `onEvict` (`:69–80`); prune evicts aged + calls `onEvict` only when evicted>0 (`:143–159`); corrupt-JSON hydrate starts empty without throwing (`:83`); `getAll()` reflects queue after `notify` AND (regression) returns `[]` after a hydrate-populates-but-no-notify path (snapshot-init gap `:30/:162`); both constructor forms (legacy storage vs options object, `:37–51`). Via `makeQueuedMessage` + fake `QueueStorage` — libDocs: — depends: W1-L1-01-FACT
- [x] **W1-L1-11-M3-COVERAGE** (config) GREEN: remove `offlineQueue.ts` from `coveragePathIgnorePatterns` — files: `museum-frontend/jest.config.js` — acceptance: WHEN coverage runs after M3 lands THE SYSTEM SHALL include `features/chat/application/offlineQueue.ts` (delete `:46`) AND the branch threshold (78, `:62`) SHALL still pass; WHEN insufficient THE SYSTEM SHALL extend M3, never lower the threshold — libDocs: — depends: W1-L1-10-M3-TEST
- [x] **W1-L1-14-M5-TEST** (test) RED: M5 timeout-budget binding (`runWithRetry` [500,2000,8000] vs profile latency) — files: `museum-frontend/__tests__/lib/retry.budget.test.ts` — acceptance: WHEN `runWithRetry` runs against a `withNetworkSim`-wrapped op per profile THE SYSTEM SHALL honour `DEFAULT_BACKOFF_MS [500,2000,8000]` (`retry.ts:34`, Nth retry waits `backoff[N-1]`); map a profile latency > `AXIOS_TIMEOUT_MS(15000)` to a retried Timeout AppError (`isRetryableError` true, `:19–20`); prove total op calls = `min(attempts, backoff.length+1)`. ENFORCED: binds `runWithRetry`/`retry.ts`, NOT the axios interceptor (`150*(retryCount+1)`, max 2, `httpClient.ts:341/321`). FakeClock + injected sleep — libDocs: — depends: W1-L1-05-HELP-B-CODE
- [x] **W1-L1-15-M6-TEST** (test) RED: M6 compression decision math unit (consumes the compression cluster's pure module) — files: `museum-frontend/__tests__/features/chat/compressionDecision.pure.test.ts` — acceptance: WHEN `compressionDecision.pure` resolves outputs for a DataMode THE SYSTEM SHALL: 'normal'→no-op (1600px JPEG passthrough); 'low'/edge→TWO outputs (D1) uploaded+persisted (~1024px WebP ~q60 ~150KB) + local enhancement (~1280px WebP ~q70 250–400KB); graceful fallback ladder WebP→JPEG q55@1024px→raw URI; assert the quantified edge target (~150KB) and normal untouched. FAILS until `features/chat/application/compressionDecision.pure.ts` exists (OWNED by D1 cluster — this is the unit test only) — libDocs: expo-image-manipulator — depends: W1-L1-00-REG-DEP *(this is the same file as `W1-D1FE-01`; keep ONE test file — merged: D1 cluster owns the production module, this M6 entry is satisfied by `W1-D1FE-01`. Editor: do not create a duplicate test.)*
- [x] **W1-L1-16-M7-TEST** (test) RED: M7 geo/museum-search + auth degraded retry caps — files: `museum-frontend/__tests__/features/chat/degradedPaths.retry.test.ts` — acceptance: WHEN a museum-search/geo query runs under 'edge'/'2g' THE SYSTEM SHALL retry transient failures up to the React Query `shouldRetry` cap then surface an actionable AppError (never hang); WHEN Unauthorized (401/403) THE SYSTEM SHALL NOT retry (`isRetryableError` false, `retry.ts:22–26`); assert retry counts + immediate Unauthorized propagation. Uses `withNetworkSim` + `makeNetInfoSnapshot` — libDocs: @tanstack/react-query — depends: W1-L1-05-HELP-B-CODE, W1-L1-01-FACT
- [x] **W1-L1-17-ESLINT-TEST** (test) RED: assert ESLint forbids `app/` importing `shared/testing/**` — files: `museum-frontend/__tests__/lint/sharedTestingBoundary.test.ts` — acceptance: WHEN ESLint lints an `app/` fixture importing `@/shared/testing/withNetworkSim` THE SYSTEM SHALL report a `no-restricted-imports` error; WHEN a `__tests__` file imports the same THE SYSTEM SHALL NOT error. Programmatic ESLint over inline fixtures (no real `app/` file changed). FAILS — libDocs: eslint, typescript-eslint — depends: —
- [x] **W1-L1-18-ESLINT-CODE** (code) GREEN: add `no-restricted-imports` boundary for `shared/testing/**` — files: `museum-frontend/eslint.config.mjs` — acceptance: WHEN code under `app/**` imports `@/shared/testing/*` or relative paths into `shared/testing` THE SYSTEM SHALL fail lint via a scoped `no-restricted-imports` block (mirror existing scoped file-config style `eslint.config.mjs:136–161/200–205`), message pointing to the L1-harness boundary; test files allowed; `npm run lint` stays clean — libDocs: eslint, typescript-eslint — depends: W1-L1-17-ESLINT-TEST

### Cluster C — D2 Idempotency (FE↔BE)

> **Merge note:** the FE M4 (`W1-L1-12`/`W1-L1-13`) and `W1-IDEM-05`/`W1-IDEM-06` are the SAME deliverable from two cluster plans. Merged into the single `W1-IDEM-05`/`W1-IDEM-06` below. BE-side is `W1-IDEM-01..04`.

- [x] **W1-IDEM-01** (test) RED: BE dedup store unit tests — files: `museum-backend/tests/unit/chat/idempotency.store.test.ts` — acceptance: WHEN given the same Idempotency-Key twice THE SYSTEM SHALL report already-seen on the 2nd call and return the stored original result; distinct keys→distinct; WHEN the entry TTL elapsed (via `inMemoryCacheService` expiry / `__setNowForTest`) THE SYSTEM SHALL treat as new; WHEN the backing `CacheService` throws THE SYSTEM SHALL FAIL-OPEN (treat as new, never throw). Import not-yet-existing `@modules/chat/useCase/message/idempotency.store`→compile fail. Reuse `tests/helpers/cache/inMemoryCacheService.ts` (setNx/get/set TTL) + DRY key fixtures; mirror `guardrail-friction.store.test.ts` — libDocs: ioredis — depends: —
- [x] **W1-IDEM-02** (code) GREEN: implement BE dedup store (dual in-process + Redis-via-CacheService, setNx reserve + result replay) — files: `museum-backend/src/modules/chat/useCase/message/idempotency.store.ts`, `museum-backend/src/config/env.ts`, `museum-backend/src/config/env.types.ts` — acceptance: WHEN `configureIdempotency({cache})` is called at chat-module boot THE SYSTEM SHALL select the Redis adapter if a `CacheService` is injected else in-process; expose `remember(key, ()=>Promise<result>)` that returns the cached result on hit, on miss reserves via `setNx` then stores the serialized result under `idempotency:result:<key>` with a short TTL (new env `IDEMPOTENCY_TTL_MS` default ~10min, bounded), and FAILS OPEN on any cache error. Make W1-IDEM-01 green without editing it; `@modules/@shared` aliases — libDocs: ioredis — depends: W1-IDEM-01
- [x] **W1-IDEM-03** (test) RED: BE idempotency middleware tests on message-create path — files: `museum-backend/tests/unit/chat/idempotency.middleware.test.ts` — acceptance: WHEN `POST /sessions/:id/messages` is called twice with the same Idempotency-Key THE SYSTEM SHALL invoke `chatService.postMessage` exactly once and return the identical 201 body both times; distinct keys→two creations; no header→behaves as today (next(), zero overhead); the dedup key SHALL be scoped by `Idempotency-Key + userId + sessionId` so a 400/Zod short-circuit can't burn the key (CLAUDE.md mutating-middleware ordering). Build via `tests/helpers/chat/chatTestApp.ts` + spy `ChatService` (DRY). Asserts not-yet-existing middleware import → fail — libDocs: express, ioredis — depends: W1-IDEM-02
- [x] **W1-IDEM-04** (code) GREEN: implement BE idempotency middleware + mount on message-create + CORS header — files: `museum-backend/src/modules/chat/adapters/primary/http/middleware/idempotency.middleware.ts`, `museum-backend/src/modules/chat/adapters/primary/http/routes/chat-message.route.ts`, `museum-backend/src/app.ts`, `museum-backend/src/modules/chat/chat-module.ts` — acceptance: WHEN mounted THE SYSTEM SHALL read the Idempotency-Key header and, if present, wrap `createPostMessageHandler` so a repeated key replays the stored 201 result (captured from `res.status(201).json(result)` at `chat-message.route.ts:92`) instead of re-running `postMessage`; mount in `createMessageRouter` AFTER `isAuthenticated`+rate-limiters+`llmCostGuard`, BEFORE upload/handler; add `'Idempotency-Key'` to CORS `allowedHeaders` (`app.ts:144–151`, next to `X-Data-Mode`); call `configureIdempotency({cache})` in `chat-module.ts:719` alongside `configureGuardrailFriction`. PROD-SAFE (not a fault injector, no prod-refusal). Run `gitnexus_impact` on `createMessageRouter`/`createPostMessageHandler` before editing; report blast radius. **CORS-array merge hazard with W1-GZIP-06** — coordinate — libDocs: express, cors, ioredis — depends: W1-IDEM-03
- [x] **W1-IDEM-05** (test) RED: FE forward `queued.id` as Idempotency-Key on resync + widen `peek()` (merges M4) — files: `museum-frontend/__tests__/hooks/useOfflineSync.idempotency.test.ts`, `museum-frontend/__tests__/api/chatApi.send.idempotency.test.ts` — acceptance: WHEN `useOfflineSync` flushes a queued item on reconnect THE SYSTEM SHALL call `chatApi.postMessage` with `idempotencyKey === queued.id`, exactly once per item even across a simulated flapping toggle that re-fires the effect; WHEN `postMessage` builds the request with `idempotencyKey` set THE SYSTEM SHALL include header `Idempotency-Key:<id>` (assert via `httpRequest`/`chatApiMocks.postMessage` mock) and omit it when unset. FAILS today because `peek()` return type omits `id` (`useOfflineSync.ts:21`) and `postMessage` sets no key. Uses `makeQueuedMessage` (DRY) — libDocs: axios, react — depends: W1-L1-01-FACT
- [x] **W1-IDEM-06** (code) GREEN: FE add `idempotencyKey` to `postMessage`, forward `queued.id`, widen `peek()` (merges M4) — files: `museum-frontend/features/chat/infrastructure/chatApi/send.ts`, `museum-frontend/features/chat/application/useOfflineSync.ts`, `museum-frontend/features/chat/application/useOfflineQueue.ts` — acceptance: WHEN `PostMessageParams.idempotencyKey` is provided THE SYSTEM SHALL merge `{'Idempotency-Key':<key>}` into `httpRequest` headers (alongside `X-Data-Mode`, `send.ts:143–149`) for BOTH JSON and multipart branches, omit when undefined; WHEN `useOfflineSync` flushes THE SYSTEM SHALL pass `currentItem.id` — REQUIRES widening `peek()` in `UseOfflineSyncParams` (`useOfflineSync.ts:21`) and `useOfflineQueue.peek` (`useOfflineQueue.ts:72`) to expose `id:string`, and adding `idempotencyKey?` to `PostMessageParams` (`send.ts:20–37`) as OPTIONAL (so non-resync callers incl. `sendMessageStreaming.ts:63`/`sendMessageSmart` are untouched). Run `gitnexus_impact` on `PostMessageParams`/`postMessage`/`peek` before editing (HIGH blast-radius shared contract). Make W1-IDEM-05 green without editing it — libDocs: axios, react — depends: W1-IDEM-05
- [x] **W1-IDEM-07** (test) BE+FE round-trip e2e (rides existing non-required backend e2e) — files: `museum-backend/tests/e2e/chat/idempotency-message-create.e2e.test.ts` — acceptance: WHEN the same Idempotency-Key is POSTed twice to `/api/chat/sessions/:id/messages` against a real app+DB THE SYSTEM SHALL persist exactly one chat message and return the same messageId both times; distinct keys→two messages. Integration/e2e harness (`tests/helpers/integration`) + DRY fixtures — libDocs: express, pg — depends: W1-IDEM-04, W1-IDEM-06

### Cluster D — D1 adaptive image compression (FE)

- [x] **W1-D1FE-01** (test) RED: `compressionDecision.pure` unit tests (M6 adaptive decision math) — files: `museum-frontend/__tests__/features/chat/compressionDecision.pure.test.ts` — acceptance: WHEN resolved DataMode is 'low'/edge THE decision SHALL return `{upload:{maxDimensionPx:1024,format:'webp',quality:~0.60,targetBytes:~150000}, localDerivative:{maxDimensionPx:1280,format:'webp',quality:~0.70,targetBytes 250000..400000}}`; WHEN 'normal' THE decision SHALL return the legacy single output (1600px JPEG target 2_700_000, no localDerivative); WHEN `webpSupported=false` THE upload SHALL fall back to `{format:'jpeg',quality:~0.55,maxDimensionPx:1024}`. Imports the not-yet-existing pure module → FAILS. (This is the single M6 test — W1-L1-15 is satisfied by this file.) — libDocs: expo-image-manipulator — depends: W1-L1-00-REG-DEP
- [x] **W1-D1FE-02** (code) GREEN: implement `compressionDecision.pure.ts` (pure, two-output adaptive decision) — files: `museum-frontend/features/chat/application/compressionDecision.pure.ts` — acceptance: WHEN given `(resolvedDataMode, webpSupported)` THE pure function SHALL return the upload+optional-localDerivative descriptor matching W1-D1FE-01; no React/expo imports (pure, like `chatSessionLogic.pure.ts`); exposes `IMAGE_PROFILES` with a `.normal` entry usable as a backwards-compatible default; the 9 existing `imageUploadOptimization` tests remain untouched — libDocs: expo-image-manipulator — depends: W1-D1FE-01
- [x] **W1-D1FE-03** (test) RED: adaptive `optimizeImageForUpload` WebP@1024 + JPEG fallback + two-output, signature backwards-compat — files: `museum-frontend/__tests__/infrastructure/imageUploadOptimizationAdaptive.test.ts` — acceptance: WHEN `optimizeImageForUploadAdaptive(uri, edgeProfile)` is called THE SYSTEM SHALL invoke `manipulateAsync` with `format:'webp'` + resize to 1024 longest-side, returning a `.webp` URI ~150KB; WHEN WebP manipulation rejects THE SYSTEM SHALL retry `format:'jpeg'` q~0.55@1024 returning a `.jpg` URI; WHEN local derivative requested THE SYSTEM SHALL also emit the 1280px ~q70 derivative URI. NEW file (existing `imageUploadOptimization.test.ts` is FROZEN — do NOT edit); the `SaveFormat` mock must include `WEBP:'webp'`. FAILS until W1-D1FE-04 — libDocs: expo-image-manipulator, expo-image — depends: W1-D1FE-02
- [x] **W1-D1FE-04** (code) GREEN: extend optimizer backwards-compatibly (sibling adaptive fn / overload) — files: `museum-frontend/features/chat/application/imageUploadOptimization.ts` — acceptance: WHEN called with one arg THE SYSTEM SHALL behave exactly as today (1600px JPEG target 2_700_000, string return, all 9 frozen tests green); WHEN called via the adaptive function with an edge profile THE SYSTEM SHALL produce a `.webp` URI via `SaveFormat.WEBP` @1024px q~0.60 toward ~150KB, on WebP failure fall back to JPEG q~0.55@1024, never blocking upload (raw URI last resort); the adaptive fn returns a shape carrying `uploadUri` + optional `localDerivativeUri` WITHOUT changing the legacy string-returning export. Consumes `compressionDecision.pure.ts`. Make W1-D1FE-03 green — libDocs: expo-image-manipulator, expo-image, expo-file-system — depends: W1-D1FE-03
- [x] **W1-D1FE-05** (sentinel) RUNTIME-CHECK: verify expo-image-manipulator emits valid WebP on iOS+Android (red-phase gate) — files: `museum-frontend/__tests__/infrastructure/_webp-emission-runtime-note.md` — acceptance: WHEN W1 red phase runs THE team SHALL execute a one-shot device/sim `manipulateAsync({format:SaveFormat.WEBP})` on a fixture and confirm the output is valid WebP (magic bytes `RIFF....WEBP`) AND smaller than the JPEG equivalent; if unreliable on a platform THE decision SHALL default that platform to the JPEG q55@1024 fallback path (~220KB, still ~11x). `SaveFormat.WEBP` is statically present but native encoder fidelity is unverifiable in Jest — document the result; do not ship WebP-by-default for a failing platform — libDocs: expo-image-manipulator — depends: W1-D1FE-04
- [x] **W1-D1FE-06** (test) RED: `useImagePicker` reads `useDataMode` and passes profile to optimizer — files: `museum-frontend/__tests__/hooks/useImagePickerDataMode.test.ts` — acceptance: WHEN `useDataMode().resolved==='low'` and the user picks/takes a photo THE hook SHALL call the adaptive optimizer with the edge profile (assert via mock `arg[1]`); WHEN 'normal' THE hook SHALL call with the normal profile (legacy). NEW file (existing `useImagePicker.test.ts` FROZEN); mock `useDataMode`/DataModeProvider. FAILS until W1-D1FE-08 — libDocs: react, @react-native-community/netinfo — depends: W1-D1FE-04
- [x] **W1-D1FE-07** (test) RED: `useCompareImagePicker` reads `useDataMode` + emits correct `.webp` MIME via `describeImage` — files: `museum-frontend/__tests__/hooks/useCompareImagePickerDataMode.test.ts` — acceptance: WHEN `resolved==='low'` THE compare picker SHALL call the adaptive optimizer with the edge profile AND the returned `CompareImageFile.type` SHALL be `'image/webp'` (because the optimized URI ends in `.webp`, via `describeImage`→`normalizeImageMimeTypeFromExtension`, `_internals.ts:29–31`); WHEN WebP fell back to JPEG THE type SHALL be `'image/jpeg'`. FAILS until W1-D1FE-08 — libDocs: react, expo-image-picker — depends: W1-D1FE-04
- [x] **W1-D1FE-08** (code) GREEN: wire both pickers to `useDataMode()` + adaptive optimizer — files: `museum-frontend/features/chat/application/useImagePicker.ts`, `museum-frontend/features/chat/application/useCompareImagePicker.ts` — acceptance: WHEN a photo is picked/captured THE picker SHALL resolve the profile from `useDataMode()` (`DataModeProvider.tsx:101`) and pass it to the adaptive optimizer; the local derivative (when present) is handed to the D4 capped-cache cluster, the upload URI to `selectedImage`/`CompareImageFile`; failure of optimization still falls back to the raw URI (never blocks upload). Make W1-D1FE-06 + W1-D1FE-07 green — libDocs: react, @react-native-community/netinfo, expo-image-picker — depends: W1-D1FE-06, W1-D1FE-07, W1-D1FE-05

### Cluster E — D4 durable carnet re-download (BE contract-lock) + capped local cache (FE)

- [x] **W1-D4-BE-01** (test) RED: durable carnet image re-download contract test (existing BE path) — files: `museum-backend/tests/unit/chat/durable-image-redownload.test.ts` — acceptance: WHEN a persisted chat message image is requested via `POST /api/chat/messages/{messageId}/image-url` by its owner THE SYSTEM SHALL return a signed re-download URL (200); WHEN by a non-owner THE SYSTEM SHALL deny via `ensureMessageAccess`; WHEN `GET /api/chat/messages/{messageId}/image` is called with a valid signed token THE SYSTEM SHALL serve the durable image (302 for `s3://`, streamed bytes for `local://`). Author it to PIN the post-wipe fresh-signed-URL + ownership-boundary + signed-token-TTL guarantee so red is GENUINE (the existing path already exists — if everything passes immediately, downgrade BE-02 to a no-op regression-lock and report honestly, do NOT fabricate a failure). DRY chat fixtures (`tests/helpers/chat`) — libDocs: typeorm, express — depends: —
- [x] **W1-D4-BE-02** (code) GREEN: lock/harden durable re-download path — files: `museum-backend/src/modules/chat/adapters/primary/http/routes/chat-media.route.ts`, `museum-backend/src/modules/chat/useCase/audio/chat-media.service.ts` — acceptance: WHEN W1-D4-BE-01 runs THE SYSTEM SHALL pass with the existing persistence + signed-URL path; production change is MINIMAL (likely none beyond comments). If a real gap surfaces (ownership boundary on `image-url`, signed-token TTL too long for post-wipe), fix the smallest unit. Run `gitnexus_impact` on `getMessageImageRef`/`getMessageImageRefBySignedToken` before any edit; report blast radius. No new endpoint (persistence is NOT absent — verified) — libDocs: typeorm, express — depends: W1-D4-BE-01
- [x] **W1-D4-FE-01** (test) RED: capped image-cache policy unit (LRU + maxAge + dedicated quota eviction math) — files: `museum-frontend/__tests__/infrastructure/imageCachePolicy.test.ts` — acceptance: WHEN total cached image bytes exceed the DEDICATED cap THE SYSTEM SHALL select LRU entries for eviction until under cap; WHEN an entry exceeds `maxAge` THE SYSTEM SHALL mark it evictable regardless of LRU order; WHEN a single image larger than the cap is admitted THE SYSTEM SHALL reject admission (never evict everything for one oversized item); eviction is a PURE function over `{key,sizeBytes,lastAccessMs,createdMs}[]` returning keys-to-delete (no expo-file-system in this test). FAILS (module absent). Add `makeCacheEntry()` factory under `__tests__/helpers/factories` (`docs/TEST_FACTORIES.md`) — libDocs: — depends: —
- [x] **W1-D4-FE-02** (code) GREEN: pure image-cache policy module — files: `museum-frontend/features/chat/application/imageCachePolicy.pure.ts` — acceptance: WHEN W1-D4-FE-01 runs THE SYSTEM SHALL pass; export DEDICATED cap + maxAge constants and a pure `selectEvictions(index,{capBytes,maxAgeMs,nowMs,incomingSizeBytes?})` returning `{evictKeys,admit}`; no React, no expo-file-system import; `@/` alias — libDocs: — depends: W1-D4-FE-01
- [x] **W1-D4-FE-03** (test) RED: `offlineImageStorage` becomes a capped cache (admit/evict/rehydrate via FS) — files: `museum-frontend/__tests__/infrastructure/offlineImageStorage.test.ts` — acceptance: WHEN a derivative is written and the cache is over its dedicated cap THE SYSTEM SHALL delete LRU files (`deleteAsync`) until under cap and update its index; WHEN `getInfoAsync` reports a size THE SYSTEM SHALL account it against the cap; WHEN the cache is wiped (all files gone) THE SYSTEM SHALL return null for a previously-cached key (resolver falls through to server re-download); existing persist/cleanup behaviors retained. EXTEND the existing FROZEN test ONLY by ADDING describe blocks (keep current assertions byte-stable); mock `expo-file-system/legacy` as today, add `getFreeDiskStorage`/size to the mock. FAILS first — libDocs: — depends: W1-D4-FE-02
- [x] **W1-D4-FE-04** (code) GREEN: turn `offlineImageStorage.ts` into a capped LRU/maxAge cache — files: `museum-frontend/features/chat/application/offlineImageStorage.ts` — acceptance: WHEN images are persisted THE SYSTEM SHALL track an index (`key→{sizeBytes,lastAccessMs,createdMs}`) under a DEDICATED dir (`documentDirectory/offline-images/`, distinct from TTS `cacheDirectory/tts/`), enforce the cap via `imageCachePolicy.pure.selectEvictions` on write, support touch-on-read (LRU), expose `getCachedImage(key)`/null-on-miss; PRESERVE existing `persistOfflineImage`/`cleanupOfflineImage`/`cleanupOfflineImages` signatures for `useOfflineQueue.ts` callers (run `gitnexus_impact` first — no breaking change). Index persisted via `writeAsStringAsync`. Frozen W1-D4-FE-03 + legacy tests pass — libDocs: — depends: W1-D4-FE-03
- [x] **W1-D4-FE-05** (test) RED: carnet image resolver fallback chain + post-wipe rehydration — files: `museum-frontend/__tests__/features/chat/useCarnetImageSource.test.tsx` — acceptance: WHEN a local derivative exists in the capped cache THE SYSTEM SHALL prefer it (no network); WHEN the cache misses (cold or post-wipe) THE SYSTEM SHALL request a FRESH signed URL via `getMessageImageUrl(messageId)` then re-download the durable server copy and repopulate the capped cache; WHEN `getMessageImageUrl` fails THE SYSTEM SHALL surface the `message.image.url` server fallback without throwing. Mock `getMessageImageUrl` + `offlineImageStorage`. FAILS (hook absent). Session/chat factories (DRY) — libDocs: — depends: W1-D4-FE-04
- [x] **W1-D4-FE-06** (code) GREEN: carnet image source resolver hook — files: `museum-frontend/features/chat/application/useCarnetImageSource.ts` — acceptance: WHEN W1-D4-FE-05 runs THE SYSTEM SHALL pass; hook resolves prefer-local-derivative → else fetch fresh signed URL (`getMessageImageUrl`) + re-download + repopulate capped cache, with closure-cell cancellation on unmount (mirror `useVisitCarnet.ts` `cancelledRef`); NO raw stale signed-GET replay post-wipe (always re-mint via `image-url`); `@/` alias, no eslint-disable — libDocs: — depends: W1-D4-FE-05
- [x] **W1-D4-FE-07** (code) GREEN: wire carnet/hero display to the resolver — files: `museum-frontend/features/chat/ui/ArtworkHeroCard.tsx`, `museum-frontend/features/chat/ui/CarnetSessionCard.tsx` — acceptance: WHEN the carnet/hero renders an artwork image THE SYSTEM SHALL consume `useCarnetImageSource` so the local capped-cache derivative is preferred and post-wipe re-download is transparent. Run `gitnexus_impact` on `useArtworkHero` consumers before editing. Covered at the hook level by W1-D4-FE-05 (integration seam, no new logic). Existing carnet screen already UFR-021-covered — no new `.maestro` baseline entry — libDocs: — depends: W1-D4-FE-06

### Cluster F — Request gzip (FE) + BE request-decompression middleware + zip-bomb cap

- [x] **W1-GZIP-01** (test) RED: BE request-decompression middleware unit test (gzip/deflate/br inflate + passthrough) — files: `museum-backend/tests/unit/middleware/request-decompression.middleware.test.ts` — acceptance: WHEN a request carries `Content-Encoding: gzip` (and separately deflate, br) with a compressed JSON body THE middleware SHALL replace `req` with inflated bytes so a downstream `express.json` parse yields the original object; WHEN no `Content-Encoding` THE middleware SHALL `next()` untouched (zero-overhead passthrough); WHEN an unknown token (`snappy`) THE SYSTEM SHALL reject `415 UNSUPPORTED_MEDIA_TYPE` via `errorHandler`. Drive in isolation with a fake `Readable` req/res/next; build fixtures with `zlib.gzipSync/deflateSync/brotliCompressSync`. FAILS (middleware absent) — libDocs: express — depends: —
- [x] **W1-GZIP-02** (test) RED: zip-bomb decompressed-size cap unit test (MANDATORY, load-bearing) — files: `museum-backend/tests/unit/middleware/request-decompression-zipbomb.middleware.test.ts` — acceptance: WHEN a gzip/deflate/br request inflates to MORE bytes than the cap (= `bytes(env.jsonBodyLimit)` = 1 MiB) THE middleware SHALL abort `413 PAYLOAD_TOO_LARGE` BEFORE buffering the full payload (streaming byte counter on the inflate stream, destroyed once the running total exceeds the cap — never materialise the whole bomb); WHEN inflated size ≤ cap THE SYSTEM SHALL succeed. Fixture: small gzip payload inflating to >1 MiB. Assert `{statusCode:413, code:'PAYLOAD_TOO_LARGE'}` and that the inflate stream was destroyed (peak buffered bytes bounded). FAILS — libDocs: express — depends: —
- [x] **W1-GZIP-03** (test) RED: middleware mount-ordering + CORS allowedHeaders structural test — files: `museum-backend/tests/unit/routes/request-decompression-ordering.test.ts` — acceptance: WHEN `src/app.ts` is read as a source string THE `requestDecompressionMiddleware` mount index SHALL be < the `express.json(` index in `applyGlobalMiddleware` AND > the `compression(` mount; AND CORS `allowedHeaders` SHALL include `'Content-Encoding'`. Mirrors `tests/unit/routes/middleware-ordering.test.ts` style. FAILS — libDocs: express — depends: —
- [x] **W1-GZIP-04** (test) RED: end-to-end gzipped-request round-trip integration test — files: `museum-backend/tests/integration/middleware/request-decompression-roundtrip.test.ts` — acceptance: WHEN a real `createApp()` receives a POST with `Content-Encoding: gzip` and a gzipped JSON body on a JSON endpoint THE route handler SHALL receive correctly parsed `req.body`; WHEN the same endpoint receives an over-cap bomb THE response SHALL be `413` with `{error:{code:'PAYLOAD_TOO_LARGE'}}`. supertest against `createApp()` (integration harness convention); reuse request factories, no inline entities. FAILS until W1-GZIP-06 — libDocs: express — depends: —
- [x] **W1-GZIP-05** (test) RED: FE `httpRequest` conditional gzip-encode unit (pako, low-only, JSON-only, never multipart) — files: `museum-frontend/__tests__/infrastructure/httpRequest.gzip.test.ts` — acceptance: WHEN resolved DataMode is 'low' AND the body is a JSON string/object whose serialized size > ~1024 bytes THE `httpRequest` SHALL gzip via pako, set config body to the gzipped bytes, set `Content-Type: application/json` and `Content-Encoding: gzip`; WHEN 'normal' THE body SHALL be uncompressed with NO `Content-Encoding`; WHEN FormData THE SYSTEM SHALL NEVER gzip regardless of DataMode; WHEN JSON ≤ ~1024 bytes THE SYSTEM SHALL send uncompressed. Mock `@/shared/infrastructure/httpClient` (request) like the existing `httpRequest.test.ts` + mock the `getCurrentDataMode` resolution seam. FAILS; no eslint-disable; narrow pako types — libDocs: pako — depends: —
- [x] **W1-GZIP-06** (code) GREEN: implement BE `requestDecompressionMiddleware` + mount before `express.json` + CORS `Content-Encoding` — files: `museum-backend/src/shared/middleware/request-decompression.middleware.ts`, `museum-backend/src/app.ts` — acceptance: WHEN `Content-Encoding` is gzip|deflate|br THE SYSTEM SHALL inflate via `zlib` (`createGunzip`/`createInflate`/`createBrotliDecompress`) with a STREAMING decompressed-byte counter capped at `bytes(env.jsonBodyLimit)`; on breach destroy the inflate stream and `next(new AppError({statusCode:413,code:'PAYLOAD_TOO_LARGE'}))`; unknown encoding→`next(AppError 415)`; no header→`next()` untouched. Mount via `app.use` in `applyGlobalMiddleware` AFTER `compression()` (`app.ts:163–170`, after the `res.setTimeout` setter `:172–175`) and STRICTLY BEFORE `express.json` (`:177`). Add `'Content-Encoding'` to CORS `allowedHeaders` (`:144–151`). Replace the `req` readable (PassThrough / re-emit data+end) so `express.json` reads inflated bytes. PROD-SAFE — NO `NODE_ENV` guard. Parse the `'1mb'`-style string to bytes. Run `gitnexus_impact` on `applyGlobalMiddleware` before editing `app.ts`. **CORS-array merge hazard with W1-IDEM-04** — coordinate. Make W1-GZIP-01..04 green — libDocs: express — depends: W1-GZIP-01, W1-GZIP-02, W1-GZIP-03, W1-GZIP-04
- [x] **W1-GZIP-07** (code) GREEN: add `pako` dep + implement FE conditional request-body gzip — files: `museum-frontend/shared/api/httpRequest.ts`, `museum-frontend/package.json` — acceptance: WHEN building the request config in `httpRequest`, AND body is not FormData (`isFormData` guard `httpRequest.ts:24`), AND resolved DataMode is 'low' (read via `getCurrentDataMode()`, same seam as `httpClient.ts:192–193`), AND serialized JSON byte-length > 1024 THE SYSTEM SHALL `pako.gzip` the body, set `config.data` to the `Uint8Array`, set `Content-Type: application/json` + `Content-Encoding: gzip`; otherwise uncompressed with no `Content-Encoding`; NEVER gzip multipart. Add `pako` (+ types) to `package.json` and install. Verify the RN-safe pako import (no Node Buffer at runtime) and that axios/RN transmits the `Uint8Array` as binary with the header intact. Do not break the existing `httpRequest.test.ts`. Make W1-GZIP-05 green — libDocs: pako — depends: W1-GZIP-05
- [x] **W1-GZIP-08** (sentinel) GREEN: request-decompression invariants sentinel (prod-safe, NOT prod-refused) — files: `museum-backend/scripts/sentinels/request-decompression-invariants.mjs` — acceptance: WHEN the sentinel runs (pure-Node structural string checks on `src/app.ts`, mirroring `security-headers-invariants.mjs`) THE SYSTEM SHALL exit 0 iff (a) `'Content-Encoding'` is in the CORS `allowedHeaders` block, (b) the `requestDecompressionMiddleware` `app.use` appears before `'express.json('` in `applyGlobalMiddleware`, and (c) the decompression middleware source contains NO `NODE_ENV`/production refusal (asserting prod-SAFE — guards against a future wrong coupling to the W2 L2 fault-injection prod-refusal); exit 1 with a clear message otherwise. Wire into the `pnpm lint`/sentinel chain like existing sentinels — libDocs: — depends: W1-GZIP-06

### Cluster G — dev route `force-data-mode`

- [ ] **W1-DEV-01** (test) RED: component test for `force-data-mode` dev route — files: `museum-frontend/__tests__/app/force-data-mode.test.tsx` — acceptance: WHEN the suite runs against an absent `app/(dev)/force-data-mode.tsx` THE SYSTEM SHALL fail. Cases (frozen): (1) `__DEV__===true` + params `{value:'low'}` → calls `useDataModePreferenceStore.getState().setPreference('low')` exactly once; (2) renders an expo-router `<Redirect>` (or calls `router.replace`) to `/`; (3) on unmount → calls `setPreference('auto')` (no leak); (4) `__DEV__===false` → renders `<Redirect href="/">` and does NOT call `setPreference`. `jest.mock('expo-router')` exposing `Redirect`/`router.replace`/`useLocalSearchParams` (mirror `__tests__/app/onboarding.test.tsx`); drive the real `useDataModePreferenceStore` from `@/features/settings/dataModeStore` with `beforeEach setState({preference:'auto'})` (mirror `dataModeStore.test.ts`); read via `getState().preference`, no inline store literal; `react-test-renderer` `create()`/`.unmount()`; stub/restore `global.__DEV__` for case 4 — libDocs: expo-router, zustand — depends: —
- [ ] **W1-DEV-02** (code) GREEN: implement `app/(dev)/force-data-mode.tsx` — files: `museum-frontend/app/(dev)/force-data-mode.tsx` — acceptance: WHEN mounted in a dev bundle with `musaium:///(dev)/force-data-mode?value=low` THE SYSTEM SHALL read `value` via `useLocalSearchParams<{value?:'low'|'normal'}>()`, call `useDataModePreferenceStore.getState().setPreference(value==='normal'?'normal':'low')` once on mount (default 'low'), register `useEffect(()=>()=>useDataModePreferenceStore.getState().setPreference('auto'),[])`, and `router.replace('/')` (or `<Redirect href="/">`); AND WHEN `!__DEV__` THE SYSTEM SHALL return `<Redirect href="/">` before any store mutation (defense-in-depth, mirror `offline-prompt-preview.tsx`). MUST include a top-of-file `// e2e-skip: dev-only deterministic low-data trigger route for W3 Maestro netshape flows; no user happy-path` magic comment (≥30 chars, first 20 lines) so `screen-test-coverage.mjs` passes before W3 flows exist. `@/` alias, no eslint-disable, JSDoc header mirroring existing `(dev)/` routes; import the store from `@/features/settings/dataModeStore`. Make all W1-DEV-01 cases pass byte-for-byte-frozen — libDocs: expo-router, zustand, @react-native-community/netinfo — depends: W1-DEV-01
- [ ] **W1-DEV-03** (sentinel) GREEN-verify: screen-test-coverage sentinel passes with the new dev route — files: `museum-frontend/app/(dev)/force-data-mode.tsx` — acceptance: WHEN `pnpm sentinel:screen-test-coverage` runs after W1-DEV-02 THE SYSTEM SHALL exit 0 — the new route satisfied by its `// e2e-skip:` magic comment; MUST NOT add a new entry to `.maestro/coverage-baseline.json` (UFR-021 removals-only); no other screen's status regresses — libDocs: — depends: W1-DEV-02
- [ ] **W1-DEV-04** (config) Document W3 Maestro deeplink contract for `force-data-mode` (no flow yet) — files: `museum-frontend/.maestro/MODAL_FLOWS_NOTES.md` — acceptance: WHEN a W3 author writes the netshape flows THE notes SHALL document `openLink: { link: "musaium:///(dev)/force-data-mode?value=low" }` (scheme verified `app.config.ts:40`; query form verified vs `verify-email.yaml ?token=`), that the route auto-redirects to `/` after setting preference, and self-resets to 'auto' on unmount (R6 mitigation, so flows need not `clearState`); note that a W3 flow containing the `/force-data-mode` substring ALSO satisfies the sentinel via case (b). Doc-only append — libDocs: — depends: W1-DEV-02

### Cumulative verification (UFR-022 broad scope)

- [ ] **W1-VERIFY** (sentinel) Cumulative full-suite + coverage + lint verification — files: `museum-frontend/jest.config.js`, `museum-backend/jest.config.ts` — acceptance: WHEN all W1 tasks land THE SYSTEM SHALL run FE `npm run test` (test:node + test:rn) + `npm run test:coverage` + `npm run lint` AND BE `pnpm test` + `pnpm lint`, reporting exit codes verbatim. The M4/D2 shared-contract change (`PostMessageParams`/`peek`) MUST NOT break sibling tests (`__tests__/hooks/useOfflineSync.test.ts` 14 cases, `chatSessionStrategies.pure.test.ts`); `offlineImageStorage` rewrite MUST NOT break `useOfflineQueue.ts` callers; FE branch threshold 78 held after `offlineQueue.ts` un-ignored; the two CORS-array edits (Content-Encoding + Idempotency-Key) merged without conflict; no frozen test re-hashed; both sentinels (`sentinel:net-profiles-parity`, request-decompression-invariants, `sentinel:screen-test-coverage`) exit 0. Verify the REAL gate command, read each exit code, never a global `;`-chain exit (`feedback_verify_real_gate_not_global_exit`) — libDocs: — depends: W1-REG-13, W1-L1-18-ESLINT-CODE, W1-L1-08-M1-TEST, W1-L1-09-M2-TEST, W1-L1-11-M3-COVERAGE, W1-L1-14-M5-TEST, W1-L1-16-M7-TEST, W1-L1-07-HELP-C-CODE, W1-IDEM-07, W1-D1FE-08, W1-D4-BE-02, W1-D4-FE-07, W1-GZIP-07, W1-GZIP-08, W1-DEV-04

---

## Multi-cycle progress

This is a long-running feature (UFR-022). One row per /team cycle; archived under `team-state/multi-cycle-features/weak-network-edge-resilience/`.

| Cycle | Date | Cluster(s) attempted | Tasks landed (ids) | Red→Green status | Reviewer verdict | Notes / blockers |
|-------|------|----------------------|--------------------|--------------------|------------------|------------------|
| 1 | 2026-06-01 | A — Registry keystone | W1-REG-01..13 | RED fe1/be1 → GREEN fe0/be0, sentinel0, 6 tests frozen | APPROVED 93.4 | run `2026-06-01-weak-net-registry`. Prettier `.0` desync caught (BE strips trailing `.0`); fixed in prod for byte-parity. 2 nits deferred to L3. |
| 2 | 2026-06-01 | B — L1 harness + M1/M2/M3/M5/M7 + ESLint boundary | W1-L1-00..18 (excl. M6/W1-L1-15→cluster D; M4→cluster C) | RED 9 suites → BLOCK-TEST-WRONG (6 noUncheckedIndexedAccess in frozen tests) → fresh-red nonNull() guards → GREEN 3681 tests + lint0 + coverage branch≥78 | APPROVED 91.6 | run `2026-06-01-weak-net-l1harness`. Green found+fixed real prod bug: resolveDataMode referenced runtime netinfo enums (undefined under mock)→literal strings. Hook gap: untracked red tests skip tsc until green touches tracked files. |
| 3 | 2026-06-02 | C — D2 Idempotency (FE↔BE) | W1-IDEM-01..07 + SEC-keycap | RED 5 → BLOCK-TEST-WRONG (tsc guard) → red-fix → GREEN BE 11/11 FE 6/6 → +SEC hardening +IMPORTANT fix (red→green each) | APPROVED 89.85 | run `2026-06-01-weak-net-idempotency`. Security MEDIUM (unbounded key→sha256 cap) + reviewer IMPORTANT (in-process null-as-HIT→null=miss) both FIXED in-session. Broad BE fails = harness-boot timeout under load (NOT regression; --runInBand 6928 pass). gitnexus_impact: PostMessageParams 44 importers, OPTIONAL non-breaking. |
| 4 | 2026-06-02 | D — D1 image compression (FE) | W1-D1FE-01..08 + M6 (W1-L1-15) | RED 4 suites (tsc-on-tests, lesson applied) → GREEN → BLOCK-TEST-WRONG (sibling useImagePicker.test contract) → sibling-migrate → review CHANGES_REQUESTED (deprecated manipulateAsync) → red(mock→context API)+green(swap) → APPROVED 91.9 | APPROVED 91.9 | run `2026-06-02-weak-net-img-compress`. Adaptive: 150KB WebP upload + 250-400KB local derivative (→D4). Context API (non-deprecated) for new code; legacy optimizeImageForUpload untouched. FE full suite 3700 green. |
| 5 | 2026-06-02 | E — D4 carnet re-download + capped cache (FE) + BE lock | W1-D4-BE-01/02 + FE-01..07 | RED (fe modules absent; BE 6/6 pass-honest) → GREEN (capped cache 64MB dedicated, useCarnetImageSource, ArtworkHeroCard/Modal wired; BE no prod change) | APPROVED 90.65 | run `2026-06-02-weak-net-carnet-cache`. BE re-download pre-existed (R9 false) → regression-lock only. 2 eslint-disable declared (getFs lazy-require + cancel-guarded set-state). CarnetSessionCard not wired (no Image, anti-gold-plating). FE 3715 green. |
| 6 | 2026-06-02 | F — gzip + BE decompression | W1-GZIP-01..08 | RED → GREEN → BLOCK-TEST-WRONG (async zlib next() + supertest gzip-serialization) → red-fix → APPROVED 92.0 | APPROVED 92.0 | run `2026-06-02-weak-net-gzip`. PROD-SAFE streaming zip-bomb cap (destroys mid-inflate before materialization, security-proven). node:zlib (no BE dep), pako FE dep. Sentinel request-decompression-invariants wired pre-push+mirror. BE broad 737 green. 1 NIT (pako alpha order). |
| 7 | _TBD_ | G — dev route force-data-mode (FE) | — | — | — | next (final W1) |
| … | | | | | | |

---

## Out of scope (W2/W3)

- **L2 fault-injection middleware** (`museum-backend/src/shared/net-shaping/` fault layer) — W2; D3 OFF-unconditionally-in-prod, no escape hatch.
- **`net-fault-prod-guard.mjs` sentinel** (prod-refusal enforcement for the fault layer) — W2; keep its name distinct from W1's `net-profiles-parity.mjs`.
- **Layer 3 Toxiproxy** integration (consumes `toToxics` mapper shipped in W1) — W3.
- **W3 Maestro netshape flows** (`net-chat-edge.yaml` / `net-image-upload-edge.yaml` / `net-auth-geo-edge.yaml`) that deeplink the `force-data-mode` dev route — W3 (W1 carries the `// e2e-skip:` until they exist).

---

## Open risks for the editor

- **R1 number drift** — mitigated by `W1-REG-09` sha256 parity test + `W1-REG-12` sentinel + `W1-REG-03`/`W1-L1-08-M1` `expectedDataMode` self-test (the load-bearing anti-lie guard). M1 cannot be green until the registry's numbers are ratified.
- **Registry purity** — `networkProfiles.ts` must import NO React/NetInfo (owns plain string-literal enums). `W1-REG-01` asserts via source-grep; a careless green importing `NetInfoStateType` for the enum breaks the boundary — reviewer must catch.
- **Byte-identical vendoring fragility** — prettier/eslint --fix can silently desync the FE↔BE copies (CLAUDE.md frozen-test/lint-staged gotcha). `W1-REG-12` hashes WHOLE files; reviewer must confirm both files are pinned to identical prettier output, OR switch the sentinel to delimited-region hashing.
- **Flapping under-proof** — `flapping.expectedDataMode='low'` must be exercised in BOTH the offline window (`isConnected:false`→`:56`) AND the online 3g-lossy window (→`:58–63`), or the contract is under-proven (`W1-REG-03`).
- **M4/D2 HIGH blast-radius shared contract** — `PostMessageParams`/`postMessage`/`peek` are also on the live chat send path (`sendMessageStreaming.ts:63`/`sendMessageSmart`). New field MUST be OPTIONAL; run `gitnexus_impact` first; `W1-VERIFY` scopes broader than the M4 test (`feedback_scoped_review_misses_contract_breakage`).
- **Helper-B timeout source** — the >15000ms→Timeout mapping MUST source `AXIOS_TIMEOUT_MS` from `httpClient.ts:172`, never hardcode, or the two drift.
- **WebP encoder fidelity** — `SaveFormat.WEBP` is static but per-platform native encoder reliability is unverifiable in Jest (`W1-D1FE-05` runtime gate); failing platform → JPEG q55@1024 fallback (~220KB). WebP wire-MIME depends entirely on the optimized URI ending `.webp`; a raw-original-URI fallback ships the un-recompressed original (acceptable, never blocks upload — make the decision explicit).
- **Frozen-test traps** — `imageUploadOptimization.test.ts` (9 cases, mocks `SaveFormat` w/o WEBP, single-arg), `offlineImageStorage.test.ts`, and `useImagePicker.test.ts` are FROZEN. EXTEND via NEW files / NEW describe blocks; editing them = `BLOCK-TEST-WRONG`. Never bundle red+green in one editor pass (`feedback_bundled_red_green_frozen_test_gap`); write tests already-formatted (lint-staged auto-fix can trip the freeze hook).
- **D4 BE genuine red** — `W1-D4-BE-01` may pass immediately (path already exists). Author it to assert a NOT-YET-GUARANTEED property (post-wipe fresh-URL ownership boundary / signed-token TTL ceiling); if everything passes, downgrade `W1-D4-BE-02` to a no-op regression-lock and report honestly — do NOT fabricate a failure (UFR-013).
- **D4 post-wipe re-mint** — the `GET …/image` signed token is TTL-bound; the resolver (`W1-D4-FE-06`) MUST always re-mint via the ownership-checked `POST …/image-url` on cache miss, never replay a stale signed GET (else silent 401/expiry). `offlineImageStorage` rewrite MUST keep its 3 exported fn signatures stable (`useOfflineQueue.ts` resync) — `gitnexus_impact` mandatory. Two FS roots: TTS = `cacheDirectory` (OS-evictable), image = `documentDirectory` (survives launches, cleared by user data-wipe → triggers the intended server re-download); the dedicated image cap MUST NOT share TTS quota. A single oversized image must not evict the whole cache; maxAge bounds growth even under cap (`W1-D4-FE-01/02`).
- **D1→D4 derivative handoff** — the 250–400KB local enhancement derivative is produced at `W1-D1FE-08` and consumed by `W1-D4-FE-04`/`-06`; the derivative-URI contract (separate from upload URI) must be agreed cross-cluster to avoid double-eviction or a missing cap.
- **Gzip stream substitution** — `express.json` reads from the `req` readable; the decompression middleware must correctly substitute the inflated stream and enforce the cap on the STREAMING inflate (not post-buffer) — a naive full-buffer impl defeats the zip-bomb cap (security phase scrutiny). The middleware is PROD-SAFE and runs in prod — must NOT carry any `NODE_ENV` refusal nor be confused with the W2 L2 prod-guard (`W1-GZIP-08` sentinel asserts this).
- **Gzip RN binary body** — axios/RN must transmit the `Uint8Array` body as raw binary with `Content-Encoding: gzip` intact (known RN footgun); verify at runtime in red phase. The ~1KB threshold + low-only gating must read the SAME `getCurrentDataMode()` seam as the rest of the app, not a fresh NetInfo read.
- **CORS-array double edit** — `W1-IDEM-04` (Idempotency-Key) and `W1-GZIP-06` (Content-Encoding) both edit `app.ts:144–151`; sequence them to avoid a conflict-merge; `W1-VERIFY` confirms both land.
- **Dev-route leak (R6)** — the persisted `force-data-mode` low-pref can leak across same-sim flows if a crash interrupts between `set('low')` and unmount; the `useEffect` cleanup mitigates the normal path. The `// e2e-skip:` comment is load-bearing (≥30 chars, first 20 lines) — a green-phase auto-format that shortens it turns the sentinel red. Adding the new file to `coverage-baseline.json` is FORBIDDEN (UFR-021) — reject at review. Component test must reset the shared zustand singleton in `beforeEach`; verify jest-expo defines `__DEV__` for the case-4 toggle.
- **ESLint boundary net-new** — no `no-restricted-imports` rule exists today; `W1-L1-18` adds a scoped block mirroring `eslint.config.mjs:136–161`. `shared/testing/`, `mulberry32`, `FakeClock`, `makeQueuedMessage`, `pako`, and `lib-docs/pako/` are ALL net-new — doc-cache must fetch pako docs in red/green.