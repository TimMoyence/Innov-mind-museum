{
  "lastUpdated": "2026-03-26",
  "enrichments": [
    {
      "id": "PE-001",
      "rule": "jest.Mocked<T> au lieu de as any pour les repos mock dans les tests. Pattern: const mockRepo = { method: jest.fn() } as jest.Mocked<PortInterface>",
      "source": "S7-S8: as any en hausse (+27), corrige V1.1 wave-c (64->0)",
      "inject_when": "agent ecrit des tests avec mocks de repositories ou services injectes",
      "severity": "HIGH",
      "r4Validation": "CONFIRME ACTIF — 0 as-any total, pattern respecte"
    },
    {
      "id": "PE-002",
      "rule": "Apres toute reecriture totale d'un fichier, verifier via git show HEAD~1:path/file vs version actuelle que le contenu original est preserve",
      "source": "Docker-compose audit Run #1: 3 regressions par reecriture (APP_VARIANT, CloudFlare, Redis)",
      "inject_when": "agent reecrit un fichier entier (Write au lieu de Edit)",
      "severity": "HIGH"
    },
    {
      "id": "PE-003",
      "rule": "tsc --noEmit DOIT passer AVANT de declarer les tests verts. Un test qui passe Jest mais echoue tsc casse le CI.",
      "source": "S8: TS2556 AuthContext.test.tsx — tests Jest OK mais tsc --noEmit FAIL -> CI casse",
      "inject_when": "agent ecrit ou modifie des tests",
      "severity": "CRITICAL"
    },
    {
      "id": "PE-004",
      "rule": "Pour les hooks React Native avec etat metier, verifier que les donnees survivent a un changement de vue/navigation. Utiliser un store ou hook persistant, pas useState local.",
      "source": "ZenFirst runs 5-6: donnees perdues au changement de step car useState local",
      "inject_when": "agent cree un composant ou hook avec etat metier",
      "severity": "HIGH",
      "r4Validation": "CONFIRME ACTIF — 2 violations critiques trouvees: useChatSession (C2) + ConversationsScreen (C3). Enrichment pertinent et necessaire."
    },
    {
      "id": "PE-005",
      "rule": "Ne pas utiliser des chemins relatifs avec grep/rg dans les agents. Toujours utiliser le chemin absolu ou cd vers la racine du repo d'abord.",
      "source": "Audit 2026-03-24: comptages as any inexacts (25/18 vs reel 21/17) a cause de chemins relatifs + hook RTK",
      "inject_when": "agent fait du comptage ou de la recherche dans le codebase",
      "severity": "MEDIUM"
    },
    {
      "id": "PE-006",
      "rule": "Les .env locaux sont gitignores — ne JAMAIS classifier comme vulnerabilite securite. Seules les cles commitees dans le repo sont des vulnerabilites.",
      "source": "Audit 2026-03-24: faux positif cle API classee CRITIQUE alors que .env gitignore",
      "inject_when": "agent fait un audit securite",
      "severity": "HIGH"
    },
    {
      "id": "PE-007",
      "rule": "Pour la coverage branches, les tests unitaires ont un plafond (~55%). Au-dela, il faut des tests integration supertest pour les routes HTTP Express.",
      "source": "S8: gap 55%->60% impossible sans supertest, routes HTTP = 0% branches",
      "inject_when": "agent travaille sur la coverage ou ecrit des tests pour des routes Express",
      "severity": "MEDIUM"
    },
    {
      "id": "PE-008",
      "rule": "Avant d'implementer un item feature/refactor, verifier si le code/la feature existe deja dans le codebase (grep + read). Si existant, retirer du scope.",
      "source": "R9: 6/14 items deja implementes, travail inutile planifie",
      "inject_when": "Tech Lead prepare le plan d'un run feature ou refactor",
      "severity": "HIGH"
    },
    {
      "id": "PE-009",
      "rule": "Les cles i18n doivent etre presentes dans TOUS les fichiers de locale (8 langues pour museum-frontend, 2 pour museum-web). Apres ajout d'une cle, verifier tous les fichiers locales.",
      "source": "R5-R6: cles ajoutees en EN/FR mais pas verifiees dans les autres langues",
      "inject_when": "agent ajoute ou modifie des cles de traduction",
      "severity": "MEDIUM"
    },
    {
      "id": "PE-010",
      "rule": "Le FINALIZE KB est OBLIGATOIRE a chaque fin de run. Mettre a jour les 7 fichiers team-knowledge/*.json. Un run sans FINALIZE est un run incomplet.",
      "source": "R7-R9: KB gaps — velocity-metrics, estimation-accuracy, agent-performance manquent 3 runs",
      "inject_when": "Sentinelle recoit le message FINALIZE",
      "severity": "CRITICAL"
    },
    {
      "id": "PE-011",
      "rule": "Dans museum-web (Next.js 15), les composants sont Server Components par defaut. N'ajouter 'use client' que pour l'interactivite (useState, useEffect, onClick). Extraire les parties client dans des fichiers separes.",
      "source": "R5-R6: pattern etabli — LoginForm client extrait de page.tsx server",
      "inject_when": "agent cree ou modifie des fichiers dans museum-web/",
      "severity": "MEDIUM"
    },
    {
      "id": "PE-012",
      "rule": "FlashList v2 (2.x) uses auto-measurement — no estimatedItemSize prop. contentContainerStyle only supports padding + backgroundColor. Use ItemSeparatorComponent for gaps (not contentContainerStyle.gap).",
      "source": "R11: design assumed v1 API, tsc caught missing prop during dev self-verification (agent scope creep — FlashList was outside mandate)",
      "inject_when": "agent works with FlashList or migrates from FlatList",
      "severity": "MEDIUM"
    },
    {
      "id": "PE-013",
      "rule": "Agent MUST NOT commit code, write KB files, update sprint tracking, or perform FINALIZE. These are Tech Lead/Sentinelle responsibilities only. Agent mandate must include explicit hors-scope for commits and KB writes.",
      "source": "R11: Frontend Architect committed 3 unauthorized commits including fake R12 FINALIZE with incorrect KB data",
      "inject_when": "every agent mandate — hors-scope section",
      "severity": "CRITICAL"
    }
  ],
  "r4Summary": {
    "validated": ["PE-001", "PE-004"],
    "noNewEnrichments": true,
    "notes": "R4 audit confirmed PE-001 (0 as-any maintained) and PE-004 (2 critical violations found). No new enrichments needed — existing set covers R4 findings."
  },
  "r10Summary": {
    "newEnrichments": ["PE-008", "PE-009", "PE-010", "PE-011"],
    "total": 11,
    "notes": "R10 process audit added 4 new enrichments. PE-008 (feature existence check) is HIGH — prevents wasted work on already-implemented features. PE-010 (KB FINALIZE) is CRITICAL — prevents KB drift."
  },
  "r11Summary": {
    "newEnrichments": ["PE-012", "PE-013"],
    "total": 13,
    "notes": "R11 added PE-012 (FlashList v2 API, from agent scope creep finding) and PE-013 (CRITICAL — agent scope creep guard, agents must not commit/KB/FINALIZE)."
  }
}
