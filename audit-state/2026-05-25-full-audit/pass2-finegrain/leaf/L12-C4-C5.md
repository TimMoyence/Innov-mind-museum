# L12 — Pass 2 fine-grain audit : P0.C4 / C4b / C5 (feature-gates)

**Scope** : `docs/ROADMAP_PRODUCT.md` items P0.C4, P0.C4b, P0.C5.
**Branch/HEAD** : `dev` @ `1fb32f5bafc5ada0b97e7ce10af39d02834df8af` (verified `git rev-parse HEAD`).
**Method** : fresh-context re-derivation. No trust in prior audit markers/comments. Every claim verified by `Read`/`Grep`/test-run with path:line. UFR-013 honesty.

---

## P0.C4 — Seed démo Bordeaux + Q-codes + monument + retrait Q-codes Paris

**VERDICT : DONE-DEV (code present, tests green) — MAIS deux claims de prose roadmap FAUX + un nettoyage Paris NON FAIT.**

### Vérifié OK (code conforme à l'intention)

- **Bordeaux trio Q-codes** présents dans `museum-backend/scripts/seed-museums.ts` :
  - Musée d'Aquitaine `Q3329534` — `seed-museums.ts:114`
  - CAPC `Q2945071` — `seed-museums.ts:124`
  - La Cité du Vin `Q16964634` — `seed-museums.ts:134`
- **Monument Pont de Pierre** (cas in/out musée "dehors") : `seed-museums.ts:143-152`, `wikidataQid: 'Q1773424'` (l.151). Trap artiste `Q1576946` explicitement évité (test l'asserte).
- **Schema** : colonne `wikidata_qid` sur l'entité — `museum.entity.ts:40-41` (`varchar(16)`, `nullable`, `unique`). Migration `1779381393403-AddWikidataQidToMuseums.ts` présente.
- **Idempotence** : `.orUpdate(['wikidata_qid'], ['slug'])` — `seed-museums.ts:274` (backfill Q-codes sur rows existantes ; PAS `.orIgnore()`).
- **Tests** : `tests/integration/seed/seed-museums.qid.test.ts` — **8/8 PASS** (run `pnpm jest --runInBand`, 2026-05-25). Pin les 4 Q-codes + idempotence + colonne SQL + anti-trap Q1576946.

### DIVERGENCES (claims roadmap réfutés par le code)

1. **FAUX : roadmap l.134 affirme `scripts/seed-pilot-museums.sh` « N'EXISTE PAS (claim audit antérieur faux) ».**
   Le fichier **EXISTE** sur `dev` HEAD :
   - `git ls-files scripts/seed-pilot-museums.sh` → résout (exit 0).
   - Fichier `6880 bytes`, exécutable, dernier commit `f6335fe52` (2026-05-18 "feat(w4) ... B2B-pilot").
   - C'est le claim « N'EXISTE PAS » de la roadmap qui est faux, pas l'audit antérieur qui le citait.

2. **NON FAIT : retrait Q-codes Paris.** Les vrais Q-codes Paris (data, pas docstring) vivent dans `scripts/seed-pilot-museums.sh` et n'ont PAS été retirés :
   - `seed-pilot-museums.sh:56-62` : `[louvre]="Q19675"`, `[orsay]="Q23402"`, `[pompidou]="Q193554"`.
   - `seed-pilot-museums.sh:158-172` : boucle `for slug in louvre orsay pompidou` → `catalog-ingest.ts --museum=$qid` (ingère réellement les 3 musées Paris).
   - Header (l.4) : *"the 3 pilot museums (Louvre, Orsay, Pompidou) used for the launch weekend test"* — contredit directement le re-scope V1 (démo Bordeaux, 0 pilot contracté, B2C-first). C'est du dead/contradictoire code "B2B-pilot weekend" pré-NorthStar.

3. **IMPRÉCIS : roadmap l.134 dit les Q-codes Paris « à retirer sont dans `catalog-ingest.helpers.ts` + `seed-museums.ts` ».**
   - `seed-museums.ts` : les 6 musées Paris (Louvre/Orsay/Pompidou/Orangerie/Rodin/Petit Palais, l.27-80) n'ont **AUCUN** `wikidataQid` → rien à retirer côté data.
   - `catalog-ingest.helpers.ts` : `museumQid` est un **paramètre**, pas une donnée hardcodée. Seule occurrence `Q19675` = exemple docstring (l.163). Rien à retirer non plus.
   - Le vrai siège des Q-codes Paris (à enterrer, UFR-016) est `seed-pilot-museums.sh` — non cité par la roadmap.

### Debt C4

- **TD-C4-PILOT-SCRIPT** : `scripts/seed-pilot-museums.sh` (Louvre/Orsay/Pompidou "pilot weekend") = artefact pré-NorthStar contradictoire avec V1. À enterrer (UFR-016) OU re-cibler Bordeaux. Roadmap l.134 le déclare à tort inexistant → corriger le texte roadmap.
- **TD-C4-PARIS-QID** : si la politique est "0 Q-code Paris", purger `seed-pilot-museums.sh:56-62` (+ docstring `catalog-ingest.helpers.ts:163`). Sinon documenter pourquoi ces Q-codes survivent.

---

## P0.C4b — Décision image-compare Aquitaine seul

**VERDICT : COHÉRENT (décision documentée), mais "Aquitaine-only" = décision OPÉRATIONNELLE, PAS un gate code.**

- Décision documentée en commentaire code : `seed-museums.ts:101-105` — *"Only Musée d'Aquitaine has Wikidata-ingest-viable artwork data (133 rows P195+P18). CAPC + Cité du Vin keep their Q-code for cards/maps/geoloc but no auto-ingest (D-SCOPE-WAVEA decision)."* Cohérent avec roadmap l.135.
- **Pas de gate programmatique** : `museum-backend/scripts/catalog-ingest.ts` n'a AUCUNE liste hardcodée de musées (`grep Q3329534|Q2945071|Q16964634|MUSEUMS|pilot` → vide). Il ingère le `--museum=<QID>` passé. "Aquitaine seul" tient donc uniquement par discipline d'opération (ne lancer l'ingest que sur Q3329534), pas par code.
- **Nuance honnêteté** : `scripts/seed-pilot-artwork-knowledge.ts:25,50-90` seed du **knowledge texte placeholder pour les 3** Bordeaux (Aquitaine/CAPC/Cité du Vin), pas seulement Aquitaine. C'est cohérent (carto+knowledge pour les 3 ; image-compare Wikidata pour Aquitaine seul) mais à ne pas confondre : "Aquitaine seul" ne concerne QUE l'ingest image-compare C3, pas le knowledge.
- C2 license mapping (`catalog-ingest.helpers.ts:53-117`, `mapLicenseUriToSlug`) testé : `tests/unit/chat/visual-similarity/catalog-ingest.helpers.test.ts` **11/11 PASS** (run 2026-05-25).

### Debt C4b

- **TD-C4b-NO-GATE** (mineur) : "Aquitaine-only ingest" non enforced par code → risque opérateur lance l'ingest sur CAPC/Cité du Vin (≈0 œuvre exploitable → `no_visual_neighbor` systématique). Optionnel : allow-list QID dans `catalog-ingest.ts` ou warning.

---

## Doc-anchors `c4b-sparql-counts.md` / `c2-license-uris.md` (UFR-024)

**VERDICT : CASSÉS — confirmé. Référencés dans du code shippé, JAMAIS committés, AUCUN sentinel ne les attrape.**

- `git ls-files '*c4b-sparql-counts*' '*c2-license-uris*'` → **VIDE** (non trackés sur dev).
- `find` → 0 fichier réel. Le chemin pointé = `.claude/skills/team/team-reports/working/2026-05-21-p0-feature-gates/` = répertoire **éphémère/disposable** (CLAUDE.md "`working/` = disposable"), supprimé.
- Références dans du **code shippé** (pas seulement doc) :
  - `museum-backend/scripts/seed-museums.ts:101`, `:139`
  - `museum-backend/scripts/catalog-ingest.helpers.ts:46`, `:108`
  - `museum-backend/tests/unit/chat/visual-similarity/catalog-ingest.helpers.test.ts:33`
  - `museum-backend/tests/integration/seed/seed-museums.qid.test.ts:7`, `:14`, `:104`
  - (+ refs prose dans `docs/ROADMAP_PRODUCT.md:87,135` et tasks multi-cycle)
- **Le sentinel `doc-anchor-check.mjs` cité dans CLAUDE.md + plusieurs audits N'EXISTE PAS** (`find -name doc-anchor-check*` → vide). Le seul sentinel UFR-024 réel = `scripts/sentinels/roadmap-claim-resolves.mjs`, qui **ne scanne QUE `docs/ROADMAP*.md`** (header l.1-25), PAS les anchors en commentaires de code. Donc ces 8 anchors morts en code passent silencieusement tous les gates.
- Les valeurs (Q-codes, mappings licence) sont elles **correctes** ; seul l'anchor de provenance est cassé.

### Debt anchors

- **TD-UFR024-ANCHORS** : committer `c4b-sparql-counts.md` + `c2-license-uris.md` dans un emplacement tracké (ex `museum-backend/docs/`) et corriger les 8 réfs, OU retirer les réfs. **Et** : soit créer le `doc-anchor-check.mjs` promis par CLAUDE.md, soit corriger CLAUDE.md (claim sentinel inexistant = méta-anchor cassé).

---

## P0.C5 — Telemetry Plausible (conversion funnel KR4)

**VERDICT : DONE-DEV — CÂBLÉ E2E de bout en bout. Tests verts. Un seul gap : env var FE non documentée.**

### Chaîne E2E vérifiée (FE → BE → Plausible)

1. **Consent banner monté à la racine** : `museum-frontend/app/_layout.tsx:32` (import) + `:231` (`<ConsentBanner />`). Donc consent peut réellement être granted en prod (sinon events no-op à jamais).
2. **Hook consent** : `shared/analytics/useAnalyticsConsent.ts` — tri-state `unset/granted/denied`, opt-in par défaut (`cachedStatus='unset'` l.44), persistance AsyncStorage (`STORAGE_KEY` l.27), fail-closed. Reader sync `hasAnalyticsConsent()` l.47-49.
3. **Emitter FE** : `shared/analytics/plausible.ts` — `trackFunnelEvent` gate consent AVANT fetch (`if (!consentPredicate()) return` l.107), PII strip (l.26-50), header `X-Musaium-Analytics-Consent: granted` (l.128), POST vers proxy BE.
4. **Events paywall (3/4) câblés FE** :
   - `paywall_modal_shown` — `features/paywall/application/PaywallProvider.tsx:82`
   - `paywall_cta_clicked` — `features/paywall/ui/QuotaUpsellModal.tsx:143`
   - `paywall_email_captured` — `features/paywall/ui/QuotaUpsellModal.tsx:161`
5. **Event `quota_exceeded` (4/4) câblé BE** (pas FE — conforme roadmap "côté middleware") : `src/shared/middleware/monthly-session-quota.middleware.ts:101-102` (`getTelemetryPort().emit({ name:'quota_exceeded', ... })`).
6. **Route BE proxy** : `POST /api/telemetry/funnel` — `src/modules/telemetry/adapters/primary/http/routes/funnel.route.ts`. Gate consent-header fail-closed 403 (l.85-101), rate-limit 60/600s/IP (l.38-42), Zod (l.107), PII strip (l.70-78, 119), `port.emit` (l.114). Montée : `src/shared/routers/api.router.ts:405` (`router.use('/telemetry', telemetryRouter)`).
7. **Adapter secondaire** : `src/modules/telemetry/adapters/secondary/plausible.adapter.ts` → POST `/api/event` (shape plausible.io), `X-Forwarded-For` forward (bot filter).
8. **Env BE** documentée + validée : `PLAUSIBLE_DOMAIN` / `PLAUSIBLE_ENDPOINT_URL` dans `.env.example:267-269` + `.env.production.example:127-129`, lues via `src/config/env.ts:531-532`.

### Tests (run 2026-05-25, verbatim)

- BE : `pnpm jest --testPathPattern="telemetry|funnel"` → **6 suites / 24 tests PASS** (`funnel-consent-header`, `funnel-quota-exceeded`, `funnel-route-integration`, + orchestrator cache-telemetry specs).
- FE : `npm test` (harness propre) → `PASS __tests__/analytics/plausible-consent.test.ts`, **284/284 pass, 0 fail**. (NB : `npx tsx --test` direct échoue — artefact de runner-path, PAS un vrai fail ; le harness FE compile vers `.test-dist/` d'abord.)

### DIVERGENCE / gap

- **Env var FE non documentée** : `resolveDomain()` (`plausible.ts:85-86`) lit `EXPO_PUBLIC_PLAUSIBLE_DOMAIN` SANS fallback ; si absente → `trackFunnelEvent` no-op silencieux (`return` l.113). Or `EXPO_PUBLIC_PLAUSIBLE_DOMAIN` (et `EXPO_PUBLIC_PLAUSIBLE_ENDPOINT_URL`) **ne figurent dans AUCUN `.env*.example` FE** (`grep` sur `.env.example`/`.env.production.example`/`.env.preview.example` → 0 hit ; seul `EXPO_PUBLIC_API_BASE_URL` est documenté, qui sert de fallback à l'endpoint mais PAS au domain). → en prod, sans set manuel, les 3 events FE no-op silencieusement. Le code est correct (fail-safe), c'est la **doc/config qui manque**.
- **Roadmap claim "ancien claim grep posthog/plausible=0 est FAUX"** : confirmé, Plausible est bien câblé (réfutation correcte de l'ancien claim).

### Debt C5

- **TD-C5-FE-ENV-DOC** : ajouter `EXPO_PUBLIC_PLAUSIBLE_DOMAIN` (+ optionnel `EXPO_PUBLIC_PLAUSIBLE_ENDPOINT_URL`) aux `.env*.example` FE, sinon funnel FE silencieusement inerte en prod (no-op à l.113). Sévérité : MOYEN (KR4 dashboard reste vide sans ça, mais 0 crash).
- (info) "dashboard Plausible à finaliser" (roadmap) = action ops externe, hors code.

---

## Synthèse verdicts

| Item | Verdict | Note clé |
|---|---|---|
| **C4** | DONE-DEV ✅ (tests 8/8) | 2 claims roadmap FAUX (script "n'existe pas" → existe ; retrait Paris non fait, mauvais fichiers cités) + dead pilot script Paris à enterrer |
| **C4b** | COHÉRENT ✅ | Décision documentée, mais "Aquitaine-only" = opérationnel, pas de gate code |
| **C5** | DONE-DEV ✅ (BE 24/24, FE 284/284) | Câblé E2E complet ; seul gap = env var FE `EXPO_PUBLIC_PLAUSIBLE_DOMAIN` non documentée → no-op silencieux prod |
| **doc-anchors** | CASSÉS ❌ | `c4b-sparql-counts.md`/`c2-license-uris.md` non trackés, 8 réfs en code shippé, AUCUN sentinel ne les catch ; `doc-anchor-check.mjs` (CLAUDE.md) inexistant |
