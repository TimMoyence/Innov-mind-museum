# L03 — Audit fine-grain P0.A7 / A8 / A9

- **Scope** : `docs/ROADMAP_PRODUCT.md` items P0.A7, A8, A9 (bloc P0.A, tous marqués ✅ DONE-DEV #293).
- **Branche / HEAD** : `dev` @ `1fb32f5bafc5ada0b97e7ce10af39d02834df8af`.
- **Méthode** : re-dérivation from scratch ; aucun audit antérieur cru ; preuves path:line confirmées par lecture directe (UFR-013).

---

## P0.A7 — Walk path circuit breaker

**VERDICT : ✅ CONFIRMÉ DONE (vérifié).**

Énoncé marqueur : `generateWalk` ne checkait PAS le breaker (early-return AVANT le guard default-path L249), cost containment bypassable ; fix = `checkLatencyBreakerOrThrow(site:'invokeSection'|'generateWalk')` garde les 2 paths.

Preuves (`museum-backend/src/modules/chat/adapters/secondary/llm/langchain.orchestrator.ts`) :
- `checkLatencyBreakerOrThrow` défini L137-148 : `if (this.circuitBreaker.state !== 'OPEN') return;` sinon `throw new CircuitOpenError()`.
- `checkCostBreakerOrThrow` défini L162-177 : capture `wasHalfOpen` AVANT `canAttempt()` (mutation `probeInFlight`), throw si `!canAttempt()`.
- Dans `generateWalk` (L510-) : **les deux guards sont appelés L556-560** — `checkLatencyBreakerOrThrow('generateWalk', …)` puis `checkCostBreakerOrThrow('generateWalk', …)` — **AVANT** l'appel LLM réel `invokeWalkStructured(...)` L563.
- L528-549 (avant le guard) ne contiennent QUE de l'assemblage de prompt (`buildOrchestratorMessages`, `buildSectionMessages`, `injectWalkTourGuideSection`) — aucun appel réseau LLM. Donc le guard précède bien tout coût.
- Default path conserve son propre check : L321 `if (this.circuitBreaker.state === 'OPEN')` + cost-breaker guard L334 `checkCostBreakerOrThrow('invokeSection', …)`.

**Divergence vs marqueur** : aucune fausse claim. Nuance mineure — le marqueur dit « generateWalk ne check PAS … (main path L249 check) » : sur l'arbre courant le check default-path est à L321/L334 (pas L249, drift de numérotation vs description pré-fix). Sans impact sur le verdict.

**Debt** : aucune. Couvert par RUN_ID 2026-05-21-p0-c2-cost-breaker (spec §3 R3). Note : breaker storage in-process single-instance (KISS V1) — promotion Redis = Phase 3 (déjà documentée dans `llm-cost-circuit-breaker.ts:36-38`, hors scope A7).

---

## P0.A8 — Honesty docstrings (UFR-013)

**VERDICT : ✅ CONFIRMÉ DONE (a)+(b), le code/comment dit la vérité (vérifié).**

### (a) cost-breaker docstring

`museum-backend/src/modules/chat/adapters/secondary/llm/llm-cost-circuit-breaker.ts:4-13` : le préambule UFR-013 documente explicitement que 3 anciennes assertions étaient fausses (« phase-2 stub not yet wired », « no production caller invoked recordCharge() », « fail-CLOSED enforced by callers ») et « This run wires the missing guards ». **Plus aucune chaîne "PHASE 2 NOT WIRED" mensongère.**

Vérification que la NOUVELLE docstring dit vrai (pas juste un autre mensonge) :
- `recordCharge()` est bien appelé live : `recordSectionCost` (L104-113) → `this.costBreaker.recordCharge(cents)` L113 ; invoqué default path L284 + walk path L588. ✅ La claim « recordCharge had been calling live since C9.4 » est exacte.
- `canAttempt()` est bien désormais checké : L168 dans `checkCostBreakerOrThrow`, appelé aux deux entrypoints (L334 + L557). ✅ La claim « This run wires the missing guards » est exacte.

### (b) deleteAccount comment CASCADE

`museum-backend/src/modules/auth/useCase/account/deleteAccount.useCase.ts:61-65` : « object-storage + marketing cleanup MUST run BEFORE the DB rows are wiped (R16). `chat_sessions` are removed first in deleteUser (CASCADE → `chat_messages`)… ».

Vérification contre l'impl `user.repository.pg.ts:220-237` :
- `deleteUser` ouvre une transaction (L221), **delete `chat_sessions` d'abord** (L223-228) puis `User` (L230-235). ✅ Ordre conforme au comment.
- Comment inline L222 « Chat sessions -> FK cascade: messages, artwork_matches, message_reports » corrobore le CASCADE → `chat_messages`. ✅

**Divergence vs marqueur** : aucune. Les deux docstrings sont honnêtes ET vérifiées contre le call graph réel (pas seulement réécrites cosmétiquement).

**Debt** : aucune. Préambule cost-breaker note que la suppression des fausses strings est enforcée par un grep-contract `tasks.md` — non re-vérifié ici (hors scope, pas un risque code).

---

## P0.A9 — OAuth callback `code`/`state` dans SENSITIVE_QUERY_KEYS

**VERDICT : ✅ CONFIRMÉ DONE (vérifié).**

**Divergence de PATH (marqueur trompeur)** : le marqueur cite `sentry-scrubber.ts:23-31`. Or `museum-backend/src/shared/observability/sentry-scrubber.ts` **ne définit PAS** `SENSITIVE_QUERY_KEYS` — il le **re-exporte** depuis `@musaium/shared` (L15). La définition réelle (source de vérité) est :

`packages/musaium-shared/src/observability/sentry-scrubber.ts:29-41` :
```
export const SENSITIVE_QUERY_KEYS: ReadonlySet<string> = new Set([
  'access_token', 'api_key', 'apikey',
  'code',          // L33 ✅
  'email', 'password', 'phone', 'refresh_token', 'secret',
  'state',         // L39 ✅
  'token',
]);
```
- `code` (L33) et `state` (L39) **présents**.
- Effectivement consommés : `scrubUrl` L142-157 redacte toute query-key dont `key.toLowerCase()` ∈ SENSITIVE_QUERY_KEYS (L151) → pas du dead code.
- Couverture test live : `packages/musaium-shared/src/observability/sentry-scrubber.test.ts:73,79` (Set contient code/state) + L164-169 `scrubUrl redacts code/state/email/phone query-params (R1)`.

**Divergence vs marqueur** : path `sentry-scrubber.ts:23-31` est INEXACT pour le backend (c'est un re-export, et l'ancre 23-31 pointe vers `hashEmail`/`scrubEvent`, pas la Set). La vraie ancre = `packages/musaium-shared/src/observability/sentry-scrubber.ts:29-41`. Sub-claim « subsumed by A3 » non vérifiable depuis ce scope mais le contenu fonctionnel (code+state présents et actifs) est confirmé.

**Debt** : DOC-LOW — corriger l'ancre A9 dans `ROADMAP_PRODUCT.md:101` (`sentry-scrubber.ts:23-31` → `packages/musaium-shared/src/observability/sentry-scrubber.ts:29-41`), sous peine de futur faux-négatif `doc-anchor-check.mjs`.

---

## Synthèse

| Item | Verdict | Ancre vérifiée | Divergence marqueur |
|---|---|---|---|
| A7 | ✅ DONE | `langchain.orchestrator.ts:137-148, 556-560, 563` | numérotation default-path (L321/334 vs marqueur L249) |
| A8(a) | ✅ DONE | `llm-cost-circuit-breaker.ts:4-13` + `orchestrator.ts:113,168` | aucune |
| A8(b) | ✅ DONE | `deleteAccount.useCase.ts:61-65` + `user.repository.pg.ts:220-237` | aucune |
| A9 | ✅ DONE | `packages/musaium-shared/src/observability/sentry-scrubber.ts:33,39` | **path roadmap faux** (re-export BE, vraie source = shared) |
