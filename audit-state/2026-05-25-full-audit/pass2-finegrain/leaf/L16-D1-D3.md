# L16 — Audit fresh-context P0.D1 / P0.D2 / P0.D3 (burial completeness)

- **Branche/HEAD** : `dev` @ `1fb32f5bafc5ada0b97e7ce10af39d02834df8af`
- **Scope** : vérifier la complétude des suppressions D1 (SSE résidus FE), D2 (stryker-incremental.json untracked), D3 (llama-prompt-guard supprimé) + chasser le BUG bulle-vide texte-seul (P0-FA1).
- **Principe** : aucune confiance aux marqueurs roadmap ; chaque item re-dérivé par grep/`git ls-files`/run.

---

## P0.D1 — Burial SSE résidus FE — VERDICT : ✅ BURIAL COMPLET (source) · ⚠️ BUG P0-FA1 CONFIRMÉ (consommateur)

### Burial des fichiers/flag

| Cible | Preuve | Verdict |
|---|---|---|
| `sseParser.ts` | `git ls-files \| grep -i sseparser` → **NONE** | ✅ supprimé |
| `chatApi/stream.ts` | `git ls-files museum-frontend/.../chatApi` → dir absent ; aucun `stream.ts` FE | ✅ supprimé |
| `tests/sse-parser.test.ts` | absent de `git ls-files` | ✅ supprimé |
| Fonction `isChatStreamingEnabled()` | `grep -rn isChatStreamingEnabled museum-frontend` → **0 hits** | ✅ retirée |
| Flag `EXPO_PUBLIC_CHAT_STREAMING` en **source** | `git grep` → 4 hits, **TOUS dans `__tests__/infrastructure/chatApi.test.ts:425-446`** (test-guard qui prouve que le flag NE route PAS vers un stream path). 0 hit en `features/`/`shared/`/`app/`/env examples. | ✅ retiré de la source (résidu test = bénin, intentionnel) |
| Orphan imports (`from .../stream`, `postMessageStream`, `from .../sseParser`) | grep FE → **0 orphan** | ✅ propre |

Commit burial : `134abe293` (`refactor(chat-fe): bury dormant SSE streaming path; sendMessageSmart always-sync (D1)`). Existence confirmée.

> **Note honnêteté** : le message de commit `134abe293` prétend avoir retiré « `EXPO_PUBLIC_CHAT_STREAMING` env examples » — vérifié, aucun résidu en env example (0 hit). Le seul résidu du flag est une assertion de test légitime, pas du dead-code. Claim commit = exact.

> **Hors-scope confirmé** : `museum-backend/.../langchain-orchestrator-stream.ts` existe encore côté BE — c'est du backend, hors périmètre D1 (FE residuals). Non comptabilisé.

### ⚠️ BUG bulle-vide texte-seul (P0-FA1) — **CONFIRMÉ** (trace byte-level)

**Verdict : CONFIRMÉ. Bulle assistant VIDE pour les messages texte-seul. Image/audio OK (asymétrie réelle).**

Trace reproductible (source @ HEAD) :

1. `useChatSession.ts:236-241` — stratégie d'envoi par défaut (fall-through) = `sendMessageStreaming` pour tout texte-seul (`cache` miss tombe ici aussi).
2. `sendMessageStreaming.ts:42-54` — crée un placeholder `{ id: streamingPlaceholderId, text: '' }` et **set `context.streamingIdRef.current = streamingPlaceholderId`** (L44).
3. `sendMessageStreaming.ts:62` `await context.chatApi.sendMessageSmart({…, onToken, onDone, onGuardrail})`.
4. `send.ts:169-172` — `sendMessageSmart = (deps) => async (params) => deps.postMessage(params)`. **Ignore intentionnellement `onToken`/`onDone`/`onGuardrail`** (commenté L158-164) et renvoie le `PostMessageResponseDTO` complet (sync).
5. Donc `onDone` (L81-109) **n'est jamais appelé** → `resetStreaming()` (qui remet `streamingIdRef.current = null`, `useStreamingState.ts:36-43`) **jamais exécuté**.
6. `sendMessageStreaming.ts:117` garde : `if (response && (!context.streamingIdRef.current || attempt.imageUri))`.
   - Texte-seul : `streamingIdRef.current` = `streamingPlaceholderId` (toujours truthy, jamais reset) → `!truthy = false` ; `attempt.imageUri = undefined` → `false`. **`false || false = false` → bloc fallback SKIPPÉ.**
   - Image : `attempt.imageUri` truthy → `false || true = true` → bloc exécuté → bulle remplie (`L128-148`).
7. Résultat texte-seul : le placeholder `text:''` n'est **jamais** rempli (ni par `onDone`, ni par le fallback). `streamTextRef` reste `''` (aucun `onToken`). **Bulle assistant vide.**

Confirmations annexes :
- `useStreamingState.ts:41` — seul `resetStreaming()` remet `streamingIdRef.current = null` ; aucun autre site. Pas de reset entre L44 et L117.
- `flushStreamText`/`scheduleFlush` (`useStreamingState.ts:22-34`) déclenchés uniquement par `onToken` (L79) / `onGuardrail` (L112) — tous deux morts.

**Tests verts malgré le bug (fausse confiance — UFR-013/UFR-021)** :
- `useChatSession.test.ts:34,42` mocke `sendMessageSmart` directement (le vrai façade jamais exercé).
- Test texte-seul `useChatSession.test.ts:214-246` (`'AI response'`) : asserte `sendResult===true` + `callArgs.text` mais **JAMAIS** la présence du texte dans `result.current.messages` (la bulle). → bug invisible.
- Test `onDone` `useChatSession.test.ts:773+` : `mockImplementation` appelle `params.onToken/onDone` manuellement → **exerce un callback path MORT** (le vrai `sendMessageSmart` ne les appelle pas). Faux positif structurel.
- Test image `useChatSession.test.ts:723-768` : asserte bien `assistantMsg.text==='This is a beautiful painting'` → passe car path image rempli.
- **Run exécuté** : `jest __tests__/hooks/useChatSession.test.ts __tests__/infrastructure/chatApi.test.ts` → **69 passed, 2 suites** (output verbatim). Le bug est 100 % invisible au suite FE.

**Debt P0-FA1** (launch-critique) :
- Fix garde `sendMessageStreaming.ts:117` : comme `sendMessageSmart` est désormais TOUJOURS sync (commentaire `send.ts:158-164`), la condition `!streamingIdRef.current` est fausse-par-construction → la remplacer (ex. `if (response && response.message.text)` inconditionnel, en remettant `resetStreaming()` avant), OU rétablir `resetStreaming()` au début du bloc sync.
- Ajouter un test qui **asserte le contenu de la bulle** texte-seul dans `messages` (pas seulement `callArgs`).
- Maestro happy-path texte-seul (UFR-021 : « s'affiche sans crasher » ne compte pas).

---

## P0.D2 — stryker-incremental.json untracked — VERDICT : ✅ COMPLET

| Vérif | Preuve | Verdict |
|---|---|---|
| Plus tracké | `git ls-files museum-backend/reports/stryker-incremental.json` → **vide** | ✅ |
| Gitignored | `git check-ignore museum-backend/reports/stryker-incremental.json` → **match** ("IS ignored") | ✅ |
| `reports/` propre | `git ls-files museum-backend/reports/` → vide | ✅ |

Commit : `e49b75fe5` (`chore(repo): untrack 18.5MB stryker-incremental.json build artifact (D2)`). Existence confirmée. Aucune debt.

---

## P0.D3 — llama-prompt-guard supprimé — VERDICT : ✅ COMPLET

| Vérif | Preuve | Verdict |
|---|---|---|
| Adapter/test/docker tracké | `git ls-files \| grep -i llama` → **0** ; `docker-compose*llama*` → 0 tracké + 0 sur disque | ✅ supprimés |
| Grep `src/` | `grep -rn 'llama-prompt-guard\|LlamaPromptGuard' museum-backend/src` → **0 hits** | ✅ |
| Wiring `chat-module.ts` | `grep -n 'llama\|prompt-guard' chat-module.ts` → **0 refs** | ✅ jamais wiré, confirmé |
| ADR décision | `docs/adr/ADR-051-oss-guardrail-providers-ready.md` présent | ✅ |

Commit : `eda7a0b7d` (`refactor(chat): bury never-wired llama-prompt-guard adapter (D3, ADR-051)`). Existence confirmée.

**Résidus `llama` sur disque = TOUS untracked/non-pertinents** (vérifié `git ls-files` = 0) :
- `museum-backend/dist/.../llama-prompt-guard.adapter.*` — build artifacts stales, `dist/` gitignored (`git check-ignore` confirme). Régénérables.
- `museum-backend/coverage/lcov-report/.../llama-prompt-guard.adapter.ts.html` — rapports coverage, gitignored.
- `node_modules/.pnpm/@langchain+core/.../ollama.*` + `ops/llm-guard-sidecar/.venv/.../transformers/models/llama*` — librairies tierces (LLM Guard sidecar Python, modèles transformers `llama4`/`mllama`/`code_llama`), **sans rapport** avec l'adapter supprimé.

**Debt (cosmétique, non-bloquante)** : `dist/` contient encore les artefacts de l'ancien build (untracked) ; un `pnpm build` propre les efface. Pas une régression de burial.

---

## Synthèse verdicts

| Item | Verdict | Debt |
|---|---|---|
| **P0.D1** burial source SSE | ✅ COMPLET (flag/fichiers/fonction/orphans propres) | — |
| **P0.D1** bug bulle-vide texte-seul (P0-FA1) | ⚠️ **BUG CONFIRMÉ** (trace L44→L117 ; tests verts car ils n'assertent pas la bulle texte-seul) | **P0 launch-critique** : fix garde `sendMessageStreaming.ts:117` + test bulle + Maestro |
| **P0.D2** stryker untracked | ✅ COMPLET | — |
| **P0.D3** llama burial | ✅ COMPLET (0 tracké, 0 src, 0 wiring) | cosmétique : `dist/` stale (untracked, régénérable) |

**Runs exécutés** : `jest useChatSession.test.ts chatApi.test.ts` → 69 passed (prouve l'invisibilité du bug P0-FA1).
