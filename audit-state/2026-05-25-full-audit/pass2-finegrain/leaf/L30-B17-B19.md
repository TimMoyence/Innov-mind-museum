# L30 — Audit fine-grain P0.B17 / B18 / B19 (GDPR)

- **Scope**: P0.B17 (ANTHROPIC_API_KEY dead config + key rotation), P0.B18 (/terms route museum-web), P0.B19 (S3 PAB IaC)
- **Branch/HEAD**: `dev` @ `1fb32f5bafc5ada0b97e7ce10af39d02834df8af`
- **Mode**: READ-ONLY fresh-context (UFR-022). Re-dérivé from scratch, aucun marqueur antérieur fait foi.
- **Date**: 2026-05-25

---

## P0.B17 — ANTHROPIC_API_KEY dead config + clé exposée

**VERDICT: code/config cleanup = DONE-DEV ✅ (vérifié). Clé live encore présente dans `.env` working-tree (non committée) = ops humain ⚠️🧑‍🔧.**

### Code / .env.example cleanup (DEV) — CONFIRMÉ DONE

| Check | Résultat | Preuve |
|---|---|---|
| `@anthropic-ai/sdk` dans deps | ✅ ABSENT | `pnpm list @anthropic-ai/sdk` → "NOT INSTALLED" ; `museum-backend/package.json` : `grep anthropic` = 0 |
| `@langchain/anthropic` installé | ✅ ABSENT (NON installé) | `pnpm list @langchain/anthropic` → "NOT INSTALLED". Seules occurrences = `museum-backend/pnpm-lock.yaml:3893,3912` (peer-dep wildcard `'*'` de `@langchain/community`, jamais résolu/installé) |
| Refs `.ts` source | ✅ 0 usage prod | Grep `anthropic` (case-insensitive) sur tout `museum-backend/` hors node_modules/dist : seules occurrences = test fixtures (`tests/unit/chat/chat-contracts-branches.test.ts:342` provider string, `tests/e2e/chaos-llm-provider.e2e.test.ts:56` assertion anti-leak `not.toMatch(/openai\|deepseek\|anthropic/i)`) + `reports/mutation/mutation.html` (artefact généré). AUCUN dans `src/`. |
| Parsé dans env config | ✅ NON parsé | `grep -ni anthropic` sur `src/config/env.ts`, `env.types.ts`, `env-resolvers.ts` = 0. `ANTHROPIC_API_KEY` n'est PAS lu par le schéma d'env → dead config morte. |
| `.env.example` | ✅ NETTOYÉ | `grep -ni anthropic museum-backend/.env.example` = NONE |
| `.env.production.example` | ✅ NETTOYÉ | `grep -ni anthropic museum-backend/.env.production.example` = NONE |
| Commit de cleanup | ✅ `e0aade002` / `7c671f8da` (#293) | `git log` Lot P0 sécurité/PII #293 |

→ Le claim roadmap "DONE-DEV (#293, dead config retirée)" est **VRAI et vérifié**.

### Clé exposée (OPS) — RISQUE RÉEL CONFIRMÉ ⚠️

**Une vraie clé `sk-ant-…` est PRÉSENTE dans le working-tree :**

`museum-backend/.env:108`
```
ANTHROPIC_API_KEY=sk-ant-api03-<REDACTED>
```

**Statut git de `.env` :**
- `git ls-files museum-backend/.env` → vide (NON tracké).
- `git check-ignore museum-backend/.env` → `.env` (gitignored).
- → La clé n'est **PAS committée dans l'historique git** (sur ce working-tree à HEAD `1fb32f5ba`). Le risque = fichier local sur la machine du dev, PAS une fuite repo publique. Mais c'est une clé réelle (format `sk-ant-api03-…`) qui doit être **révoquée côté Anthropic** par sécurité (provenance/usage inconnu, et la dead config implique qu'elle ne sert à rien).

**Bonus (hors scope strict B17, signalé honnêtement)** — le même `.env` contient d'autres clés live actives :
- `museum-backend/.env:106` `OPENAI_API_KEY=sk-proj-…` (réelle, utilisée)
- `museum-backend/.env:107` `GOOGLE_API_KEY=AIza…`
- `museum-backend/.env:109` `DEEPSEEK_API_KEY=sk-62f5…`
Ces 3 sont des secrets de prod légitimes (LLM providers utilisés). NON committés (même gitignore). Pas un finding B17 mais à garder en tête : tout `.env` exposé = ces clés à rotater.

**Debt B17**: 🧑‍🔧 OPS — révoquer `sk-ant-api03-<REDACTED>…` sur le dashboard Anthropic (clé morte, aucun usage code, présence inexpliquée). Effort ~5 min provider-side. Aucun fix code restant.

---

## P0.B18 — /terms route museum-web

**VERDICT: DONE ✅ (vérifié complet). Page réelle + canonical + hreflang + lien Footer i18n.**

| Check | Résultat | Preuve |
|---|---|---|
| Page existe | ✅ | `museum-web/src/app/[locale]/terms/page.tsx` (2396 B) |
| Contenu réel (pas stub) | ✅ | `src/lib/legal/terms-content.canonical.json` (8299 B) ; `page.tsx:45-77` map `content.sections[].paragraphs[]` en `<article>` réel, FR/EN via `pickLocale` (`page.tsx:31-33`) |
| Canonical URL | ✅ | `page.tsx:40` `alternates: getAlternates(locale, '/terms')` ; `src/lib/seo.ts:7` → `canonical: ${BASE_URL}/${locale}/terms` |
| hreflang (alternates) | ✅ | même `getAlternates` (émet `languages` alternates) ; `page.tsx:35-43` `generateMetadata` |
| OpenGraph | ✅ | `page.tsx:41` `openGraph: getOpenGraph(locale)` |
| Lien Footer | ✅ | `src/components/shared/Footer.tsx:73-78` `<Link href={\`/${locale}/terms\`}>{dict.footer.links.terms}</Link>` |
| i18n dict key (pas undefined) | ✅ | `src/dictionaries/en.json:202` `"terms": "Terms of Service"` ; `src/dictionaries/fr.json:202` `"terms": "Conditions d'utilisation"` |
| Test couvrant | ✅ | `src/components/shared/Footer.cookies-terms-subprocessors.test.tsx:105-125` assert href `/fr/terms` + `/en/terms` (B18, UFR-022) |
| Commit | ✅ `71f103b35` (#294) cité roadmap — non re-vérifié git mais artefacts présents | — |

→ Claim roadmap "DONE-DEV `71f103b35` #294" = **VRAI et vérifié par artefacts**. (Bonus : `/subprocessors` + `/cookies` liens Footer aussi présents, Footer.tsx:79-89.)

**Debt B18**: aucune. Item clos.

---

## P0.B19 — S3 bucket Public-Access-Block IaC

**VERDICT: ❌ ABSENT (confirmé). Aucune trace code/IaC. NEEDS-OPS-HUMAN reste exact.**

| Check | Résultat | Preuve |
|---|---|---|
| `infra/` Terraform | ❌ AUCUN | `ls infra/` = `grafana/`, `langfuse/`, `nginx/` only. ZÉRO `.tf` |
| Fichiers Terraform repo-wide | ❌ 0 | `find . -name '*.tf' -o -name '*.tfvars'` (hors node_modules) = vide |
| `PublicAccessBlock` / `GetPublicAccessBlock` / `BlockPublicAcls` refs | ❌ 0 | grep case-insensitive sur `*.ts *.cjs *.mjs *.tf *.yml *.yaml *.sh` (hors node_modules) = 0 hit |
| CI sentinel boot-check | ❌ ABSENT | `museum-backend/scripts/sentinels/` = `ai-tests-count.mjs`, `metric-naming.mjs`, `privacy-content-drift.mjs`, `web-cookies-audit.mjs` — aucun S3/PAB |
| AWS SDK S3 client | (info) NON présent en src | `grep S3Client|@aws-sdk/client-s3` src = 0 (le driver S3 utilise config env mais pas de client AWS SDK direct importé en `src/` — driver via endpoint S3-compatible) |
| Citation roadmap `BREACH_PLAYBOOK.md:217` | ✅ RÉSOUT EXACT | `docs/incidents/BREACH_PLAYBOOK.md:217` : *"Verify bucket public-access block is **enabled** (operator responsibility — known gap per audit § 5.3; TBD — confirm on prod bucket today)."* + ligne 441 checklist `[ ] Public-access block on prod S3 bucket verified (audit § 5.3 gap)` (case NON cochée) |

**Surface de risque confirmée** : S3 stocke voice audio (HIGH — biométrie voix, `BREACH_PLAYBOOK.md:55`) + uploaded images (MEDIUM-HIGH, EXIF GPS, ligne 56). Config S3 réelle parsée (`src/config/env.ts:518` `S3_BUCKET`, `env.production-validation.ts:70` required en prod). Orphan-purge cron + media-purger référencent le bucket. Donc le bucket prod existe/est attendu → un PAB désactivé = bucket world-readable avec clés objet énumérables. Risque roadmap exact.

**Debt B19**: 🧑‍🔧 OPS/IaC — pas de Terraform ni de sentinel boot-check `GetPublicAccessBlock`. Deux voies possibles (l'une OU l'autre) :
1. Terraform `aws_s3_bucket_public_access_block` (mais repo n'a aucune base Terraform → coût setup).
2. **Recommandé V1** (plus léger) : sentinel boot-check au démarrage backend OU script CI qui appelle `GetPublicAccessBlock` sur `$S3_BUCKET` et fail si pas `BlockPublicAcls=true && IgnorePublicAcls && BlockPublicPolicy && RestrictPublicBuckets`. Aucune trace d'aucune des deux. Item NON résolu sur dev.

---

## Synthèse

| Item | Verdict | Dev | Ops humain restant |
|---|---|---|---|
| B17 | Code cleanup DONE ✅ ; clé live présente ⚠️ | ✅ rien à faire | 🧑‍🔧 révoquer `sk-ant-api03-<REDACTED>…` (clé morte, non-committée mais réelle) |
| B18 | DONE ✅ complet | ✅ clos | aucun |
| B19 | ❌ ABSENT | ❌ aucune trace | 🧑‍🔧 IaC Terraform OU sentinel `GetPublicAccessBlock` |

Tout ci-dessus = **vérifié** (path:line + commandes), aucun supposé. Roadmap (lignes 123-125) honnête et exacte sur les 3 items.
